# PKUBA 小程序重建计划

## 目标

在不修改旧项目的前提下，以 Django/PostgreSQL 为唯一权威数据源，重建微信小程序和内部管理网站。新版必须复现已确认的旧业务，并修正调赛锁、容量预留、权限、记录表发布与审计缺陷。

旧系统审计与规则依据见 `PKUBA_现有小程序功能与逻辑规格.md`。与旧实现冲突时，本文件中的已确认产品决定优先。

## 当前状态

| 里程碑 | 状态 | 完成条件 |
|---|---|---|
| M0 文档与单仓库 | 已完成 | Git、GPL-3.0、开发脚本、README、CI |
| M1 纵向主干 | 已完成 | PostgreSQL → Django API → 管理站/小程序真实赛程 |
| M2 赛季与赛程 | 核心已实现，验收中 | 赛季/组别、球队与名单、场地/时段/容量、V3.3 多轮签位配置、赛程编排、统一抽签映射、三态公开/归档及人工淘汰赛待复核均已实现；真实登录态与微信端验收继续收口 |
| M3 身份与调赛 | 核心已实现，邮件外发待启用 | 领队/管理员身份、调赛事务、角色工作台、网页扫码/密码登录、任务箱和超级管理员赛程例外均已完成；业务事件已写入固定公邮 Outbox，真实 SMTP 外发须在轮换凭据并完成验收后启用 |
| M4 记录表与统计 | 已实现，真机待验收 | 唯一原图、四次识别预算、共享草稿、自动租约、两秒同步、电脑编辑器、小程序无损适配、原子发布、公开统计与 PDF/CSV/XLSX 已实现；本轮 Qwen 额度已用完 4/4，未经新授权不再调用，仍需微信开发者工具/真机交互验收 |
| M5 上线 | **NO-GO，测试收口中** | 本地存储、人工导出、归档、同一清单成对恢复和同机蓝绿脚手架已有实现；Python 依赖安全门槛已清零，正式记录表 Playwright 已通过，仍缺单栈到双栈的基线转换、空业务库首启、真实回切/成对恢复和微信真机验收 |

### 2026-08-28 DEF046/044 提交结果恢复

- 仅修已复现的客户端稳定操作键、成功正文丢失后的发布核对、调赛原申请恢复，以及批量照片部分成功后的刷新与剩余项重试；不新增后端幂等机制或改变权限、租约及发布事务。
- 验收保留原稳定键正常对照，旧“维持故障现状”的诊断断言另配正确行为用例；以少量真实 PostgreSQL/API 已提交后丢响应验证唯一写入。未变后端、布局与上一批门槛按字节绑定复用，集中跑受影响客户端测试、类型和一次构建，再交 Commit 与独立复验。

### 2026-08-28 DEF020/042 与 DEF112/037 日常页面小修（待提交与独立复验）

- 普通/跨周调赛更换比赛后，只接收当前比赛的候选、错误和加载结果，候选未就绪时不提交；任务箱切换筛选或继续分页时，旧请求不混入当前列表、游标或加载状态。
- 数据页前台刷新发现赛季/组别实际改变时清空旧榜单和分页；同一组别的正常刷新仍保留已有内容可读，不改统计来源或全局登录策略。
- 共用记录表抽屉将内边距移至 ScrollView 内层 View，保留输入、长内容滚动和只读限制；网页工作台两处启动提示移除本地环境假设，保留真实服务错误。
- 按最新提速要求，仅复用已封存的 26+5 个页面反例/对照与 5 个样式/文案检查，集中执行受影响组件、类型和构建；不重跑未改后端或扩展理论时序。所有检查使用独立 TEMP，不改日常 8088、原 dist/cache、SDK 设置或独立报告；原生验收和上线门禁仍独立保留。

### 2026-08-28 DEF110/038 小批修复（提交前返修，待最终独立复验）

- 仅统一记录表来源的上传能力与修复账户/赛程编排窄屏横溢，不夹入其他缺陷、微信工具设置或日常环境更新。
- 本场任何 publication 历史均保留普通管理员的重传限制，超级管理员换图后回到草稿也不例外。媒体、记录表队列/详情/同步共享服务端能力；两端不从状态或租约推断，文件选择后再核对。查看原图及合照/其他照片权限不变，不新增模型或迁移。
- 初版 f07 候选在提交前独立复验发现旧同步/共享请求恢复权限、缓存比赛行未同步收紧、失败暂存提前恢复三类边界，均留存原红例。r2 修复后，独立复验又发现心跳结束后切离仍获取旧租约、新详情未立即更新缓存比赛行、旧 GET/租约续接覆盖新详情三类遗漏；r2 不作为整批通过。
- r3 在网页将详情 GET 的请求顺序与服务端水位一并核对，旧响应和等待租约后的旧续接不能回退缓存，同水位的最新合法权限仍能生效。新详情立即更新原图区及对应缓存行；打开、重新载入和心跳等待后，在继续取得租约前重验当前上下文。后端沿用已通过的 r2：幂等查找只读，当前权限、赛季、来源版本和图片检查后才恢复失败暂存，合法恢复与成功重放保持。
- 独立复验又确认小程序原图错误自动重载后，同水位旧轮询/旧 GET 可恢复过时上传能力，仍归 DEF110。r4 只修小程序读取顺序与相应测试/文档；网页、后端、契约与布局保持 r3 字节。新读取共享顺序与水位，保留人工输入和来源历史，后续合法授权可恢复；不扩展全局登录、租约离页策略或原生工具设置。
- r4 原六条反例通过后，独立相邻测试仍发现旧保存回包恢复同水位权限、旧轮询失败覆盖新读取错误状态两处遗漏，r4 不作为整批通过。r5 将编辑命令回包也绑定发起顺序；同水位拒旧，真正推进服务端水位的成功写入保留，同时隔离过时轮询错误。只增量修改小程序、相应测试与本批文档；网页、后端和布局继续逐字节复用已验证候选，独立复验及最终提交绑定仍是交付门槛。
- r5 原封 17 项通过后，独立伴随控件又证实同步摘要先到会阻断完整保存正文，以及旧 actor 的高版本写回包仍可恢复过时权限；r5 不作为整批通过。r6 分开比较已知摘要水位与完整正文，允许迟到成功保存补齐人工稿及后续版本，同时按较新观察保留权限；后续合法授权仍能恢复。仅继续小程序、对应测试及文档的最小修订，原反例保留，原生与最终提交验收不由组件门槛替代。
- 账户表单、赛季选择和编排工具栏在窄屏换行；宽账户表与赛程网格只在自身容器横滚，不裁掉页面或控件，独立记录表桌面布局不变。
- 使用独立 PostgreSQL、TEMP 构建与真实浏览器验证；日常 8088、原 dist/cache、独立报告与旧参考不改。聚焦和完整门禁收口后交既有 Commit，再由独立测试绑定最终 SHA 复验；微信原生阻塞不由组件或网页通过替代。
- r2 隔离 PostgreSQL 全量 463 passed / 29 skipped，开发来源权限矩阵 32/32；独立后端专项 65/65，Ruff、迁移无漂移、OpenAPI/生成客户端字节一致。r3/r4 后端与契约内容未变，按文件字节绑定复用该证据，不声称重新执行后端全量。初版测试库连接清理警告保留为历史证据，不据后续未再出现而宣称修复了测试生命周期。
- r3 根类型检查、领域 9、管理站 250、小程序 112 与产物 4 项通过，管理站与显式 HTTPS 的微信完整构建成功，5 类非法构建地址均被拒绝且产物不变。来源权限组件 32 项及复核/编辑相邻聚焦共 65 项通过。独立任务原封 Web 20 与队列 7 项均转绿，新增上下文及队列回归另行封存；这些集合有重叠，不累加计数。r4 不用这些成绩替代小程序新增读取边界，整批仍待最终候选与提交验收。
- r4 开发验证：小程序来源/识别专项 21 项、全套 123 项、产物 4 项及根类型检查通过；原封两组 Mini 读取反例/对照 6 项转绿。显式 HTTPS 微信构建成功，5 类非法地址均拒绝且 90 个产物未变。该结果是隔离组件与构建证据，不是微信 SDK/真机验收，仍交独立测试复验。
- 真实网页验证普通/超级管理员在“曾发布后换源草稿”与从未发布两类记录的入口和原图缩放/载入；账户与编排在 320/375/390/768/1280/1600px 无整页横溢，390px 实际输入、弹窗取消、宽表内部横滚及末列编辑还原均成功，未提交业务修改。
- 上述布局与入口实测属于未变动的页面样式，r2 独立网页 20 条亦通过。r3 开发真实网页在扣留旧同步时，由真实发表推进事件而保持草稿版本不变，新 GET 即收紧能力，切到另一记录后缓存列表仍隐藏重传。新增时序矩阵、截图和资源清理记录另行封存，独立复验与最终提交绑定继续收口。

### 2026-08-28 DEF107/111 小批修复（开发验证通过，待提交后独立复验）

- 仅修复独立复验退回的中文字段校验和既存名单换号中间态冲突，不夹入其他日常问题。
- 常见校验类型与特殊日期容量等已知字段使用中文；未知类型/路径安全降级，保留业务错误、HTTP 状态、错误码和登录失效处理。
- 完整名单先校验稳定 ID、重复 ID、版本和预览确认，再在同一事务内释放变动号码并显式写回最终状态。允许互换、循环及停用/省略后复用；不删除重建球员、不放宽唯一约束、不改变历史统计，临时阶段不增加版本或额外审计。
- 隔离 PostgreSQL 全量 431 passed / 29 skipped，原封复用的名单独立反例与新增回归共 68/68；Ruff、迁移无漂移、OpenAPI/客户端重生字节一致。领域 9、管理站 218、小程序 108、产物 4 项及错误提示独立反例 32/32 通过，根类型检查与双端完整构建通过；不新增模型或迁移。
- 真实隔离网页完成互换号码预览、取消零写入、确认成功及稳定 ID/单次审计核对；空日期错误在 1440px/390px 完整显示中文，修正后预览和保存均成功。组件/构建不替代微信原生验收，既有原生连接阻塞仍待独立补验。
- 仅使用隔离 PostgreSQL 和临时构建/网页环境，不改日常 8088、原 dist/cache 或独立报告。冻结后交既有 Commit，再由独立测试绑定最终 SHA 重放反例；以上为开发侧验证，不是独立验收或上线放行。

### 2026-08-28 日常首四项（1409bcd 已提交；独立复验退回 DEF107/111，原生页面仍待补验）

- 范围仅为 SUP100/086/101/104：已有名单 UUID 预览、管理站字段错误、小程序三个调赛私有入口的身份状态、软删除历史照片归档。其他日常项目、SUP103/087 和已接受风险不夹入。
- 名单预览统一稳定 ID 的 JSON 表示；预览和确认仍绑定完整目标、版本、权限与审计。管理站逐字段错误保留状态/错误码，呈现字段和原因，不展开原始输入、上下文或堆栈。
- 普通/跨周创建及申请列表区分未登录、会话过期、权限不足、网络失败和真实空状态。登录采用固定入口白名单，成功后返回原页并保留合法申请深链接；取消/失败不跳转，403 仍到“我的”核对权限。失去会话或切换账号清除旧私有数据，迟到成功/失败/finally 不覆盖新身份；不改变全局身份恢复规则或夹入后续的同账号目标切换/翻页竞态批次。
- 历史照片时间在 CSV、ZIP manifest、任务 JSON 中统一；先验证清单可序列化再提升归档文件，不修改来源、清理前置条件或既有 Worker 并发策略。
- 隔离 PostgreSQL 聚焦 28 passed / 1 skipped、全量 414 passed / 29 skipped；合入正式源码后聚焦再次 28 passed / 1 skipped。前端聚焦 66 项、管理站 199、小程序 108、领域 9、产物完整性 4 项及根类型检查通过；管理站和微信完整构建通过。Ruff、迁移无漂移、重新生成的 OpenAPI/客户端字节一致，不新增迁移。
- 真实隔离网页已验证已有球员预览/确认 200、稳定 ID 和单次审计；空日期 422 提示在 1440/390px 可读；三类现用及历史照片经 Worker READY、浏览器下载与数据库清单/哈希一致。官方 CLI 可打开独立微信项目，但普通 SDK 连接仍报 SDKVersion/split，窗口截图仍报 SetIsBorderRequired；本批原生页面没有通过验收，不用组件/构建或上一批截图代替。
- 已按 final4ef8 独立结论最小合入本批，继续交既有 Commit 提交/推送和最终 SHA 独立复验；日常 8088、独立测试项目、原 dist/cache 与报告未改。不操作 Qwen、生产或标签，待人工参与事项最后集中处理。
- 最终 `1409bcd` / CI `33107626679` 三作业通过。独立复验确认名单 UUID 与历史照片归档定向通过，调赛身份组件/客户端通过但原生页面仍阻塞；英文校验提示未完全满足中文要求，另复现父版本既存的合法号码互换失败，分别按 DEF107/111 返修。浏览器下载事件取证仍为 PARTIAL，不用 HTTP 下载与包内容核对代替完整浏览器保存验收。

### 2026-08-28 R1/R2/R3 增量修复（4ef8f43 已提交；独立后端/组件/真实 Web 通过，Mini 待补验）

- 范围仅限独立复验退回的 DEF-105 稳定身份、网页复核迟到响应，以及 DEF-109 失败后整稿重试；不夹入日常其他问题、不改独立报告、不调用 Qwen 或操作生产。
- R1：明确绑定的球员使用稳定 ID；同名球员仅交换本场号码不会互换得分。姓名、启用状态或资格变化仍重新复核。使用真实保存→校验→发布→统计查询验证。
- R2：网页复核及后续校验/历史/异常/结束处理绑定打开代次与草稿快照，A→B、A→B→A、重新载入及较晚版本不会被旧响应覆盖，保留新输入和忙碌状态。
- R3：按最新确认规则，未发布的失败记录允许人工后重试，操作前明确整稿覆盖确认；成功覆盖，失败保留。普通管理员及超管均不能重试已发布记录，成功周期不能任意重试。队列与 Worker 落稿重读来源/任务/发布状态，保存旧稿修订并清空新稿的旧校验/身份映射。
- 隔离 PostgreSQL 专项 120/120；最终后端 405 passed / 29 skipped，Ruff、迁移无漂移；根类型检查、领域 9、管理站 185、小程序 56、产物 4 项通过。OpenAPI/客户端已同步，不新增模型或迁移。管理站生产构建与隔离微信完整构建通过；真实网页/DevTools 验收另记，不用构建代替。
- 真实隔离网页：同名换号后校验并发布，P1 仍为 2 分、P2 为 0；覆盖重试取消不入队，再次失败保留人工稿，成功整稿替换，已发布超管仍禁用重试。真实页面 A→B 迟到错误不污染 B；A 复核响应 v6 延迟，切 B 再开 A 保存 v7 后，迟到 v6 不回退新稿，后续校验只使用 v7。故障注入及识别结果均限定隔离数据，未调用模型。
- 用户开启隔离项目的本地调试后，官方微信开发者工具已完成真实页面与 API/数据库验收：我的→管理员工作台→记录表，取消重试不入队，再次失败保留人工草稿，成功整稿替换；普通管理员已发布记录只读，超级管理员取得租约后仍无重试入口；微信端实际发布后等待多轮同步也不会重新出现重试。原生覆盖提示已截图核对，确认/取消分支由官方 SDK 模拟选择，识别结果由隔离 Worker 桩提供，不代表真机手势或真实模型验收。日常 8088、原小程序 dist/cache 和独立报告未修改。
- 最终提交 `4ef8f43f55d2660bf91498d59a8f33335a37862a` 的 CI `33098736340` 三作业通过；独立复验再次通过 PG 405/29 skip、专项 120、追加后端 36、前端 250、追加前端 42 和真实 Web 身份/迟到响应/覆盖重试矩阵。独立 Mini 页面连接受 `SDKVersion undefined` 阻塞，DEF105/109 保留 PARTIAL/RETEST，不能用开发侧截图替代独立通过；总体仍 NO-GO。后续批次继续 Main → 既有 Commit → 最终 SHA 独立复验。

### 2026-08-27 批次（73d845c 已提交，独立复验退回 R1/R2）

- 保留已确认的淘汰赛 `group=NULL` 指定确认候选规则：同一赛季同一组别全部启用球队，排除双方，超级管理员精确选择。
- DEF-105：记录表不再仅因 `Game.version` 不同拒绝发布。服务端重读当前语义与名单；真实变化显示前后差异并提供绑定来源/草稿/当前语义的受控复核，保留原图、人工内容及旧识别/发布证据。明确球员身份映射，仍执行当前名单、比分、权限、租约、归档及审计保护；详见 `docs/SCORESHEETS.md`。
- 隔离 PostgreSQL 的 DEF-105 专项 35 项、最终后端全量 378 项通过，另有 29 项按环境跳过；Ruff、迁移无漂移、OpenAPI/生成客户端字节一致、根类型检查通过。共享领域 9 项、管理站 169 项、小程序 50 项及产物完整性 4 项通过；管理站与微信完整构建通过。本批不新增模型或迁移。
- 真实网页完成人工编辑→场地差异→明确复核→发布，1440px 复核区可用；官方微信自动化在 390px 真页面完成“我的→管理员→记录表”，分别验证仅版本变化直接发布及场地变化复核后发布，原图、人工姓名与 51:45 比分保留。原生确认弹窗由测试程序选择确认/取消，不代表真机手势验收；既有桌面记录表 900px 最小宽度问题没有夹入本批。最终结论仍由独立测试任务绑定提交 SHA 复验。
- 按用户授权新建赛季中段日常 QA，并于本地基本验收后接入 `localhost:8088`：2026 公开赛季 4 组别、32 队、64 场（1 场作废，公开 63 场）、384 名合成球员、99 个媒体文件、两张待发布草稿，以及 2025 归档/2027 准备赛季和三种活动调赛状态。全部 0039 迁移、10 类赛季隔离审计、三个 worker heartbeat 正常；数据库/媒体/归档冷拷贝逐文件哈希一致。只删除已核对的旧 `pkuba-wsl` 三个 QA 卷，不制作旧 QA 备份或迁回旧数据；旧参考目录、其他项目和历史备份未动。Qwen 显式禁用，未连接生产，临时微信验收会话已撤销且无残留编辑租约。
- 日常清单 11 项由独立测试先核实并冻结下一批；本批不夹入 SUP-103/087 或其他未放行问题，已接受风险不重开。继续既有 Main → Commit → 最终 SHA 独立复测流程，独立报告不入产品提交。

### 已验证实现（2026-08-21）

- 已建立 GPL-3.0 单仓库、开发/生产 Compose 骨架、PowerShell 脚本和 GitHub Actions。
- 已在真实 PostgreSQL 上完成核心迁移、演示数据、公开当前赛季及赛程 API。
- 管理网站和 Taro 小程序均通过生成的 OpenAPI 类型读取真实 API；没有用静态假数据冒充纵向切片。
- 已实现无签名 XLSX V3.3（格式 `3.3.0`）与统一在线草稿：固定“填写说明、签位定义、赛程网格”三个可见工作表；第二页只提示数据库签位字母含义且不参与业务导入。第三页按一天一行展开日期与星期，C 列起动态识别 1–64 个连续“赛季时段 + 自由文本场地”组合，支持显式“仅决赛”标记，不含隐藏赛季身份、HMAC 或逐场可调字段。
- 已实现服务器版本化 `ScheduleGridDraft`、动态列和稀疏比赛格。管理端“赛程编排”解除普通工作区宽度上限，并提供占满视口的专注模式和可收起检查器；按住鼠标左键可框选矩形区域，批量男女篮及领队调赛锁只在所选区域包含比赛时启用。比赛移动由格内独立抓手触发，避免与框选冲突并坚持只移动、不覆盖；直接输入、TSV 复制粘贴、100 步会话撤回/重做、动态列增删排序和 700ms 自动保存继续保留。XLSX 上传整表替换草稿；结构错误不修改草稿，业务错误可在线继续修正。
- 已实现草稿显式六项核对、问题分类和完整比赛预览；确认只新增小组、签位和比赛，并在单一 PostgreSQL 事务中写来源批次、赛季版本与审计。在线逐场 `leader_adjustable` 会写入正式比赛；XLSX 上传的格默认可调，确认后清空已提交草稿格而保留列。
- 已实现准备中赛季的导入重置预检与二次确认：只删除可追溯到已确认批次且仍安全的导入对象，抽签、赛果、调赛申请/预留、媒体、非准备状态或外部引用都会阻止整个事务。
- 已实现超级管理员“赛季与组别”工作区：可新建准备中赛季，采用系统默认配置或沿用历史赛季的组别、标准场地、容量和签位族；8 个稳定时段继续用于容量归属。赛程网格列不再是基础配置，也不从历史赛季复制；新空白草稿仅用 16 个便捷组合初始化，之后由管理员在网格表头自由编辑。
- 已实现超级管理员“球队与名单”工作区：准备中赛季按实际组别生成带“填写说明”的三列表格模板，上传后检查文件安全、必填项、`00`/`0–99` 号码、重名、重号、跨组重复、近似名称和签位数差异。新球队在首次导入中直接创建，不匹配历史名称；管理员逐项确认标准名称后，以整季快照在单事务中创建稳定球队/球员 UUID、增加赛季版本并写完整审计。每季只允许确认一次，此后永久关闭名单重导并改走主从在线编辑；已公开赛季的重命名、停用和既有球员修改必须先预览影响并二次确认，归档赛季只读。
- 球队与名单实现已通过真实 PostgreSQL 全量后端 75 项、管理站全量 21 项、Ruff、迁移漂移、OpenAPI 连续生成稳定性、全仓类型检查和生产构建。只读参考工作簿回归确认可检出跨组重复、`-121` 非法号码、同队重号和近似队名，未执行确认写入。本地 WSL 已应用 `0011_roster_imports_and_jersey_numbers`；真实超级管理员浏览器验收覆盖模板下载、球队搜索/组别筛选、桌面与 390px 窄屏主从布局且无控制台错误。验收全程未上传、保存或确认，当前公开北大杯仍为 57 队、146 场比赛、0 名名单球员和 0 个已确认名单批次。
- 已实现统一“抽签映射”运营工作台：小组赛/循环赛初始签位继续按组别完整预览并原子保存；任意多轮淘汰赛、半决赛、决赛和保级赛按 `(stage, round_number)` 排序并逐场编辑。每场双方完整后独立预览、独立保存；同轮下拉即时去重，后续轮次优先展示紧邻上一轮已确认胜队，选择其他启用球队必须显式确认警告。事务锁定同轮全部比赛和映射，复核赛季/比赛版本及预览哈希后，仅更新该场两个签位、正式比赛双方、版本和审计。
- 已删除独立“淘汰赛管理”、`GameWinnerFeed`、胜者关系编辑、自动晋级、历史推导填位和级联清空。已有淘汰赛正式比赛双方原样迁移为 `LEGACY_IMPORTED` 映射；发布或纠正赛果都不修改后续球队、比分、记录表、资料或调赛数据，仅动态标记受影响人工签位“待复核”。后台及小程序只读正式 `Game`，未落位显示“待抽签”，多轮使用唯一轮次键并保留待复核状态。
- 本轮模型与客户端已通过最终机器门槛：Ruff 通过，`makemigrations --check` 无漂移，OpenAPI/TypeScript 客户端同步，真实 WSL/PostgreSQL 后端共收集 221 项（192 通过、29 按环境跳过）；共享领域 4 项、管理站 129 项、小程序 14 项全部通过，全仓类型检查、管理站生产构建和 Taro `build:weapp` 成功。认证后的管理站桌面/390px及微信开发者工具真实视觉检查仍按下述边界单列，不以构建代替。
- 新赛季默认标准场地为五四东一、五四东二、五四东三；超级管理员可增删、改名、排序或停用，至少保留一个启用场地。普通调赛只从当季启用的标准场地按顺序自动分配。邱德拔及历史自由文本地点可作为具体比赛或特殊赛程网格地点保留，不会因存在历史引用而自动进入标准候选池。
- 标准时段固定预设为 8 个稳定代码，管理员通常只需核对名称和默认时间，不再手动增删。比赛同时保存标准时段和实际开赛时间；时间微调时仍按标准时段计算容量，调赛必须同时更新目标日期、标准时段与实际时间。
- 实时容量台账不另存“每日已用数”：服务端按正式比赛与 `ACTIVE` 预留即时计算。管理总览和赛季配置页均用从首场到末场、按周一至周日排列的双周历展示每日比赛数与每日剩余容量，并联动查看同一天。
- 特殊日期容量是管理员显式维护的稀疏覆盖，新赛季默认完全为空，绝不依据实际赛程反推或自动补高。旧快照中曾自动推导的 34 条记录只保留为 `LEGACY_INFERRED` 审计证据，不参与容量计算，也不出现在管理员例外列表。
- 赛季基础配置保存时由 PostgreSQL 事务重新校验版本、代码/顺序唯一性、资源引用，以及签位族的连续性、顺序和前缀唯一性；草稿或 XLSX 导入另行检查动态网格列唯一性。容量允许高于标准场地数或低于当前占用，但预览与台账会明确返回 `over_capacity`；占用只统计非 `VOID` 比赛和 `ACTIVE` 预留，`CONVERTED` 不重复计数。所有成功修改保存完整前后审计快照。
- 已实现个人管理员密码会话、一次性登录挑战、CSRF、后台改密、超级管理员账户列表、升级、受控降级、停用/恢复、最后一个有效超级管理员保护和匿名失败审计。降级只允许由另一名超级管理员执行，禁止自降级。网页登录密码或小程序管理员邀请码连续输错不会锁定，之后输入正确凭据可立即继续。
- 已完成网页扫码登录闭环：网页默认生成五分钟一次性二维码，不再使用六位校验码；二维码令牌只以摘要入库，浏览器另持有不进入二维码的会话绑定令牌。已登录管理员在小程序“我的”中扫码、核对登录提示并确认后，原浏览器才能单次换取 Django 会话。确认和消费分别原子加锁、重复确认幂等、跨账号确认冲突、过期/停用/重放全部拒绝并审计；个人用户名/密码继续作为备用入口。
- 已实现调赛领域事务：原比赛活动锁、目标容量/场地预留、普通流程自动生效、参赛手册审核、投票/终审和幂等释放，并含 PostgreSQL 并发争抢测试。日期关系 `request_type`、处理通道 `process_route` 与审核认定 `review_classification` 分离持久化；普通入口仅同周，跨周入口同时提供同周与跨周目标，且该入口提交的同周申请也必须由超级管理员审核。具体目标场地只保存在服务端预留与审计中；领队、投票球队、普通管理员和超级管理员在申请生效前只能看到目标日期与时段，接口、页面、任务箱、公邮与赛程导入冲突问题均不返回地点。申请通过并原子写入正式赛程后，场地才随正式比赛公开。
- 新处理通道启用前必须在停止旧 API/worker 写入的同一 writer fence 内完成 0039 与审计，再运行 `reschedule_route_activation_preflight`：任何非终态调赛申请，或 24 小时内仍有效且响应缺少 `process_route` 的旧调赛创建幂等记录，都会阻断激活；它们结束或过期后才能开放入口，不能把旧摘要伪装成新协议可重放。当前尚未实现 baseline conversion 自动脚本，不能把该独立命令称为已接入发布。
- 管理网站已启用“调赛处理”：普通管理员和超级管理员均可只读查看当前公开赛季的进行中/历史申请并核对原赛程、目标日期/时段、匿名化资源预留、容量、活动锁和球队确认；直批、拒绝、指定球队投票、终审和管理员取消仅超级管理员可执行。后台使用独立会话/CSRF 接口、申请版本检查和完整前后快照审计，不能从页面手动解锁或释放预留，也不能查看尚未生效的具体预留场地。
- 已实现 PostgreSQL 权威任务箱：对手确认、指定球队投票、超级管理员审核/终审、记录表 AI 成功核对和识别异常均由业务事务同步创建或关闭；任务以账号和业务状态幂等键去重，阅读只写 `read_at`，不改变待处理状态。小程序“我的”及底栏显示 1–99/99+ 红点，任务列表区分待处理/已完成并使用服务端生成的业务深链接。固定公邮 Outbox 仅允许 `pkubaoutward@163.com`，调赛每个权威状态、缺少领队等调赛异常及记录表识别最终失败均与业务写入同事务排队；同一状态或异常只生成一封，历史任务重建不补发。发送进程支持行锁领取、退避重试、进程中断恢复和耗尽后超级管理员异常任务，SMTP 失败不回滚业务。
- 已将 `Idempotency-Key` 接入调赛创建、记录表发布、赛程确认、抽签确认、赛季公开和比赛资料上传：原始键不入库，同账号/命令/键的并发请求由 PostgreSQL 事务 advisory lock 串行化，首次成功结果与业务写入同事务保存 24 小时；相同请求重放原结果，不同请求复用同键返回 409。通用公开赛程、领队申请、管理端媒体和记录表队列统一返回单页最多 100 条的分页结构；小程序对阵改用按比赛日双向游标，不再收集全部公开赛程分页。
- 任务箱与 Outbox 已部署到本地 `Ubuntu-24.04`：`0019_inbox_tasks_and_email_outbox` 已应用，公开赛季仍为 57 队、146 场，真实库保持 0 个任务和 0 封邮件；补建命令只读预览了 0 个调赛申请与 139 份既有记录表，未执行写入。统一回归为 Django/PostgreSQL 148 项通过、29 项按环境跳过，管理站 125 项、共享领域 4 项、小程序 4 项通过，Ruff、迁移漂移、OpenAPI/TypeScript、全仓类型检查和两端生产构建均通过。微信开发者工具窗口可定位且最新 `dist` 已生成，但其截图接口仍返回 `SetIsBorderRequired failed`，任务页与底栏红点的工具内视觉检查保留为人工验收项。
- 已修复重复调赛遗留的已转换场地占用：新申请通过时先锁定并释放该比赛此前的 `CONVERTED` 分配，再原子转换当前预留。标准场地和自由文本场地统一按 Unicode、空白及大小写归一化后检查冲突；名单维护预览哈希绑定完整目标球员快照；邀请码页面严格跟随后台当前选择赛季。
- 已将生命周期收敛为 `SETUP → PUBLISHED → ARCHIVED`：同一时间至多一个公开赛季，不再保存 `is_public` 或组别运营状态，也不提供“组别开放”“赛季开始”或回退按钮。调赛、资料、记录表与赛果操作统一要求赛季已公开、比赛双方已落位并满足各自业务条件；归档阻止活动流程且不可逆。唯一写入例外是超级管理员从网页明确确认后，对已有 publication 的记录表取得专用租约、重新校验并发布新版本，全程保留旧版本和审计。
- 淘汰赛不再传播胜者。上一轮正式胜队只作为后续人工签位的动态校验集合；不一致时公开接口、管理站和小程序显示待复核，但绝不自动更换正式对阵或清空任何下游数据。
- 已实现仅超级管理员可访问的“高级数据”页：静态覆盖全部 PKUBA 核心模型并显示数据库字段，响应禁止缓存；尚未生效或未成功的调赛申请、预留、幂等响应和审计快照中的目标场地是唯一动态脱敏例外。身份凭据、审计、发布和工作流模型只读。当前只有准备中且无引用的小组与签位可通过校验、影响预览、二次确认和完整审计进行有限物理修改，归档数据一律只读。列表和详情固定把名称、状态、日期、比分等人类可读字段放在前面，把关联 UUID、publication/revision、ID、哈希、令牌和密码摘要排在后面。
- 已实现不依赖 COS 的“备份与归档”工作区：赛季结构化数据 ZIP、扁平赛季照片 ZIP、未加密全系统 PostgreSQL/媒体 `tar.zst` 均由 PostgreSQL 租约 worker 后台生成；包内保留清单、版本、精确记录数和 SHA-256，24 小时过期，确认下载并外部保存后立即清理。只有不可逆归档赛季且最终数据包、照片包完整时才能提交照片清理；物理文件删除后永久保留媒体行、哈希、publication 引用和审计，读取返回 `410 MEDIA_PURGED`。
- 比赛图片上传已增加数据库持久化暂存与恢复队列：先提交暂存记录，再流式写入和校验文件，最后原子创建正式资产并替换旧图；进程在写文件或写幂等响应前后崩溃时，可用同一请求恢复到同一资产。未完成暂存会阻止部署预检、全系统备份和赛季照片清理，避免备份或切换捕获半完成文件集合。
- 已在独立 `pkuba_archive_test` PostgreSQL 和临时 Docker 卷完成真实恢复演练：应用至 `0023` 后生成 239,784 字节 custom dump/媒体 `tar.zst`，恢复到空的 `pkuba_restore_archive_20260823_0353`；命令自动核对全部表精确记录数、迁移集合和 1 个合成媒体文件 SHA-256，临时 API `/api/v1/health` 返回 200 且公开首页读取 2 个连续日期。验收后已删除临时 API、恢复库和三个演练卷，未触碰 8088 及其数据卷。
- 已在 `localhost:8088` 真实超级管理员会话完成一次当前公开赛季结构化数据包闭环：预检通过，归档 worker 生成 2,533,169 字节 ZIP 与 64 位 SHA-256，短期票据的 `Range: bytes=0-63` 返回 206/64 字节，随后立即丢弃临时包。实测同时发现并修复归档预览 UUID 直接写入 PostgreSQL `JSONField` 导致的 500，并新增领域服务与 HTTP 接口回归。
- 已建立标签驱动的同机蓝绿发布脚手架：稳定 Caddy gateway、独立 blue/green 应用 project、独立 data project、不可变 digest、部署锁、writer fence 和同一时间点的数据库/媒体/归档备份。部署和 application-only 回切由 root-only 持久事务日志保护；启动恢复按已落盘阶段确定性恢复原应用或完成候选应用，并在权威状态、Caddy reload、API/Web 稳定入口全部复核前保持维护。普通应用故障只切回兼容应用且不恢复数据；只有确认数据损坏时才允许按同一 manifest 成对恢复。当前兼容合同、服务器武装开关和 GitHub 部署变量均保持关闭，尚未完成基线转换与真实演练，不能称为一键生产发布。
- 生产健康接口已拆为仅进程存活的 `/health/live` 与检查 PostgreSQL、私有媒体、归档目录及版本元数据的 `/health/ready`；兼容 `/health` 使用 readiness 语义。生产 Caddy 通过 root 只读状态文件提供正式 503 维护响应，API 和 worker 均有容器健康/稳定检查。WSL 部署已移除旧数据自动导入、演示赛季生成和管理员重置，三类变更改为独立显式命令。
- 蓝绿 Compose/Caddy/脚本已通过结构校验；隔离 command-spy 已覆盖状态写失败、恢复写失败、第二次 Caddy reload 失败、稳定入口失败，以及部署、回切和恢复多个阶段的强制 `SIGKILL`，所有失败均保持维护、保留可解释事务且三类数据零恢复。readiness 已增加数据库、迁移、媒体/归档可写与同版本 worker heartbeat 检查。上述证据仍不等于服务器部署验收：现有单 project 服务器尚未转换为稳定 gateway + data + blue/green，自动化必须继续保持禁用。
- 已冻结旧 `ScoresheetReader` 提交 `f17b26e82cf3cd2d4878463e03c0580bd271fc31` 的工作台、领域模型、FIBA 2024 校验、固定累计分格派生语义、单页 A4 PDF 渲染和模板坐标；旧项目本身保持只读。人工只编辑得分号码，分值、节次、符号、序号及边界统一派生并由服务端重算；所有加时合并为第 5 节，不保存中间加时节末线。网页与小程序共享 Django/PostgreSQL 权威草稿，小程序的移动视图无损保留合并加时、异常分值、正式/后置犯规与未展开字段。
- 2026-08-24 已完成该冻结基线的实现与本地部署验收：真实 WSL/PostgreSQL 后端全量为 218 项通过、29 项按环境跳过，管理站 143 项、共享记录表领域 6 项、小程序 30 项通过，Ruff、迁移漂移、OpenAPI、根类型检查、管理站生产构建与 `build:weapp` 全绿。`localhost:8088` 真实超级管理员会话已核对 1–160 固定累计分格、只选择/删除登记号码、Q1–Q4 与合并 OT、只读派生最终比分/胜队、时间控件及中文人工日志；历史整项得分、全队犯规和暂停也按真实旧值中文显示，租约/心跳/同步事件不进入人工列表，控制台无警告或错误。运行态保持小图放大目标 8 MP、OpenAI SDK 网络读写超时 180 秒；139 份当前草稿、574 份修订和 140 份发布快照均未发现大于第 5 节的数据。微信开发者工具截图仍因 `SetIsBorderRequired failed (0x80004002)` 无法自动取得，真机手势、软键盘和安全区保留为人工验收项。
- V3.3 完整回归已在真实 WSL/PostgreSQL 环境通过：后端 86 项通过、29 项按环境跳过；管理站补充专注模式、鼠标框选、空选择禁用、抓手移动和表格独立缩放回归后为 36 项通过。Ruff、迁移漂移、OpenAPI 导出、TypeScript 客户端生成、全仓类型检查、管理站生产构建和 Taro 微信小程序构建全绿。
- 真实超级管理员登录态已验收重新构建后的 V3.3 页面：三步导航等宽显示，网格为 51 个日期、16 个可编辑便捷列和 816 个比赛格，工具栏包含复制、清空、撤回/重做、男女标记及领队调赛锁；决赛专用列与周末行配色正确，页面不再出现“比赛清单”“标准场地时段”或“可调政策”，控制台无警告或错误。既有 2026 赛季因没有签位族而按设计阻止模板下载；配置完整赛季的下载路由和组件交互已分别由 API 与组件测试覆盖。
- `0009_schedule_slots_and_daily_capacity` 已在真实 WSL/PostgreSQL 现有数据上完整提交：146 场比赛均有非空实际时间与场地文字，旧 `venue_id` 已移除，普通约束和活动/已转换预留的条件唯一索引均存在。当前既有 2026 赛季签位族为 0，仍会因缺少赛制而阻止新赛程确认；旧网格列是否为空不再影响 V3.3 模板或草稿。
- `0010_standard_venues_and_capacity_origin` 已应用到同一真实 PostgreSQL 数据库：当前赛季对外仅有 3 个标准场地、8 个预设时段、0 条管理员特殊容量；34 条旧自动推导覆盖被保留但已隔离。迁移不物理删除仍被历史比赛或特殊网格引用的地点。
- 容量与配置 UI 已在 `localhost:8088` 的真实超级管理员登录态验收：总览先显示“比赛与容量”、再显示“近期赛程”；赛季页显示标准场地、8 个稳定时段、容量与签位方案，且不再出现赛程网格列配置。V3.3 动态列只在“赛程编排”草稿或 XLSX 表头维护。
- 赛程编排 V3.3 的 2026 北大杯示例目标保持 51 个日期行、16 个便捷初始列、97 个签位和 146 场比赛；第一页规则与汇总、第三页表头/日期/可填格使用显式细实线边框并显示星期。管理端采用三步工作区和右侧“待排比赛/核对”检查器，确认前显式展示六项机器核对；任一项未通过都会禁用确认。
- 已在本机 `Ubuntu-24.04` WSL 内实际部署 PostgreSQL 17、Django/Gunicorn、Caddy 和 Mailpit；Windows 通过固定 `localhost:8088` 访问管理站和同源 API，小程序构建使用同一地址。
- 本地部署脚本会复用 Git 忽略的 `.env.wsl.local`，只在首次运行时询问管理员密码且不再向终端输出密码；本轮已轮换本地超级管理员密码并确认网页登录、`/auth/admin/me` 和归档存储总览接口正常。
- 已接入微信 `code2session`、短期注册票据和摘要化小程序会话令牌；账号只保存唯一昵称，不保存姓名、邮箱或手机号，同一账号可同时拥有领队与管理员身份。
- 管理员首次注册同时校验当前赛季邀请码并要求用户自行设置网页登录密码；密码至少 4 个字符，不做其他强度限制。后台可验证当前密码后修改，邀请码轮换不改变已有管理员密码。
- 已以只读方式从三份 `_2026北大杯` 公开备份重建“北大杯”：4 个组别、57 支球队、146 场比赛、142 场正式比分；个人身份、申请、照片和技术统计均未导入。
- 小程序 Logo 从旧页面实际引用的 `logo.jpg` 确定性提取原笔画并生成透明 PNG，所有小程序与管理站入口统一使用该资产。首页和赛程共用日期/时间分组，比赛只显示男甲、男乙、女甲或女乙，不重复标注“男篮/女篮”，仍以深灰/红色细标识增强区分。当前无未来比赛时展示近期赛果，不显示赛季结束状态。
- 小程序首页在 Logo 与赛季标题下首先展示覆盖首场至末场、按周一至周日补齐的赛季热图；零比赛日期明确显示 0，范围外补齐格弱化，非零比赛使用四档 PKUBA 红且始终保留数字，今日在范围内时显示描边与“今”。公开首页接口连续返回首末场之间的每一天并排除 `VOID` 比赛。
- 小程序“对阵”的赛程赛果不再下载全季比赛：公开接口以北京时间的比赛日为游标，初次最多返回焦点日前后 5 个比赛日；页面向上/向下分别增量读取，不插入“今天无比赛”、今日高亮、赛季边界或总场次说明。“回到今天”保留；今天无比赛时返回接口选定的最近焦点比赛日。返回页面时保留已加载范围和滚动位置，只重新核对该范围。
- 首页只承载赛事信息，不显示登录或账号摘要；所有微信登录、球队认领和管理员注册统一从“我的”进入，未登录时只显示一个微信登录入口。
- 管理后台“近期赛程”已复用同一信息层级：按日期和开赛时间分组，明确显示男女组别、场地、球队与比分；赛季结束后默认从最后一个比赛日向前展示。
- 已实现 `/api/v1/public/standings` 和小程序排名页：后端按正式小组赛比分计算 2/1 分，弃权计算 2/0 分，应用已确认的组内同分规则，并提供排名表及交手矩阵；2026 数据核对为 4 个组别、11 个小组、57 支球队和 126 场小组赛。
- 已完成小程序“数据”页三段式结构：顶部固定球队榜、球员榜和单场数据切换，按组别筛选并支持常用指标升降序。球队榜不提供“场次”排序，自动取齐服务端全部分页并展示所选组别的全部启用球队；球员榜不提供“出场/首发”排序，固定每页 20 人、最多 5 页，仅展示前 100 名，但表格仍可展示这些基础数值。球队榜从正式比分计算；球员榜只聚合每场当前 publication 中绑定稳定名单 ID 的纸面字段，历史 publication 不重复累计。
- 本地 2026 数据已写入 57 队、684 名确定性虚构名单球员和 138 份合成 publication；全部正常比赛均经过上传来源、识别结果应用、人工复核标记、服务端校验、warning 确认和正式发布服务，4 场弃权与 4 场未赛不生成球员记录。审计统一标记 `PUBLIC_LEADERBOARD_SYNTHETIC_SEEDED`，生产准备检查必须拒绝这批数据，正式上线前整批清空。
- 小程序用户可见文字统一以 26rpx 为下限，正文主要使用 28–30rpx；首页以赛季年份、醒目的“北大杯”标题和原 Logo 形成主视觉，不增加测试或赛季结束文案。
- 小程序会话支持服务端注销；本地令牌过期时会重新执行 `wx.login` 恢复已知 OpenID 的全部身份。领队认领和管理员邀请码注册继续共用同一微信账号。
- 小程序使用自定义底部导航，五个入口文字统一为 30rpx；“赛程”入口升级为“对阵”，在同一页面切换完整赛程赛果与各组别多轮淘汰赛对阵。“赛程赛果 / 淘汰赛”切换栏位于页面内容第一行并固定在顶部，长赛程滚动时无需返回页首。
- 首页、对阵、淘汰赛、数据单场、领队和管理员工作台的比赛入口已统一使用同一个“比赛详情”路由。详情页不依赖入口来源：先展示公开比赛内容，再静默复用本地会话或按微信 ID 恢复已注册身份；管理员从首页或管理员工作台进入都会看到完全相同的资料管理区和服务端授权的上传、重传，以及仅对比赛合照/其他照片开放的删除操作。公众先读取比赛、比分、当前 publication 单场数据和在线未删除比赛合照；两队球员数据使用两张独立表格，首发只显示独立标签。比赛合照上传或重新上传后立即公开；被替换文件软删除并保留审计。领队只在本队比赛看到当前 publication 记录表原图；管理员资料区固定在页面底部，以单列清单就地操作，不再设置照片排序、审核或重复的统一上传区；记录表原图不显示删除按钮，服务端也永久拒绝删除。
- 小程序面向用户的页面不渲染 UUID、哈希、内部比赛代码、publication ID、发布版本号、草稿版本号或识别尝试编号；管理员记录表列表只显示“打开核对 / 查看记录表 / 查看纠错”等业务动作。
- 球队认领改为两级选择：先选男甲/男乙/女甲/女乙，再选该组别中仍未认领的球队；提交时仍由服务端事务保证一人一队、一队一领队。
- 已实现公开淘汰赛接口和小程序横向吸附轮次列：只按正式 `Game`、比分、阶段与轮次返回任意多轮淘汰赛、半决赛、决赛及保级赛，标记正式胜者和人工签位待复核状态；未落位固定显示“待抽签”，不展示晋级来源，也不从上一轮赛果或旧硬编码简称推导球队。
- 已迁移旧小程序仍有效的领队流程：本队赛程、彼此独立的“普通调赛”和“跨周调赛”入口、申请列表、对手确认、指定球队确认、撤回，以及特殊原因调赛/协商失败抽签的线下说明。普通入口只列同一自然周目标；跨周入口同时列出同周和跨周合法目标，并在第一步提示参阅《参赛手册》。该入口的所有申请在对手同意后都进入超级管理员审核；管理员认定未跨轮次时可直接按普通办法批准，无需领队重提。
- 已迁移旧小程序仍有效的管理员查看流程；调赛直批/拒绝/指定投票/终审/取消和赛程纠错统一收口为超级管理员能力，并保留二次确认、版本检查、冲突预览、活动申请取消和完整审计。记录表首次发布前普通管理员与超级管理员均可上传、重传、编辑、手工录入、失败后重新识别、校验和发布；旧裁判入口不在 V1。
- 小程序管理员工作台已改为单个聚合接口：服务端直接返回身份、当前公开赛季摘要、超级管理员待处理调赛数量和最多 10 场近期比赛，不再先下载全季赛程及完整调赛申请。首次进入显示同布局骨架，从子页返回保留已有内容并局部刷新。
- 自定义底栏由五个 Tab 页在显示时主动同步选中索引；点击不再依赖当前页面底栏实例中的旧 `selected`，避免高亮与实际页面不一致。
- “我的”在本地没有会话令牌时会静默执行 `wx.login` 与 `code2session`；已注册 OpenID 自动恢复账号、领队绑定和管理员角色，只有未知 OpenID 才进入昵称注册。
- 已实现按 game ID 隔离的私有比赛媒体，并固定分为“记录表、比赛合照、其他照片”三类。记录表与比赛合照每场各一张当前图片，其他照片可多张；普通管理员与超级管理员都可上传、重传并软删除合照/其他照片，新照片直接进入兼容的 `APPROVED` 状态，不再审核或手工排序。记录表永远不可删除，首次发布前两类管理员均可重传，发布后仅超级管理员可重传或纠错。所有图片使用安全临时文件分块接收、计算真实字节与 SHA-256，并由 Pillow 验证 JPEG/PNG/WebP，不设置应用层字节上限或自定义像素上限；替换和软删除保留旧文件与审计，访问使用短期签名链接。
- 已实现 PostgreSQL 识别队列、Qwen 安全预处理、首次加三次自动重试、服务商更长 `Retry-After`、重传重置和迟到结果隔离；三段默认等待均为 30 秒。活动识别不能由用户停止或重复启动，四次耗尽后才允许人工发起新周期。识别图片链路对齐只读 `ScoresheetReader` 提交 `d631601`：8 MP 仅为小图放大目标，大图保持原生分辨率，完整 Base64 Data URI 限 `20,000,000` 字节，必要时只调 JPEG 质量且不缩放，SDK 网络读写超时为 180 秒。模型只接收整表处理图、球队名和球员姓名；不发送账号、UUID、赛季、日期、场地或其他比赛先验。
- 已实现 60 秒单编辑租约、15 秒心跳、两秒轮询、字段级差异和断网恢复。两端不显示接手、交接或强制接管按钮：首个终端自动取得编辑权，其余终端实时只读并在释放/失效后自动取得。网页完整保留冻结版 `ScoresheetReader` 全视口工作台，小程序为独立分包的原生区域式编辑器与四块纸面得分格。
- 已实现校验摘要绑定、全部 warning 一次确认和原子发布；publication、单场球队/球员统计不可变，旧发布在纠错完成前继续对外。公开统计和 PDF/CSV/XLSX 导出只包含纸面可提供字段。完整协议见 `docs/SCORESHEETS.md`。
- 归档专项此前的隔离快照已通过归档恢复专项 8 项并应用 `0022_archive_jobs_and_media_storage` 与 `0023_alter_gamemediaasset_byte_size`；API、网页、识别 worker 和归档 worker 均稳定运行。该专项的识别配置与真实冒烟证据保持原样，本轮淘汰赛重构不改写识别链路。最新统一全量数字以上述 221 项模型锁门槛为准；微信开发者工具/真机交互仍按 M4 验收边界保留。
- 本轮统一 `check-wsl` 已通过：真实 PostgreSQL 后端 130 项通过、29 项按环境跳过，管理站 44 项、记录表领域 4 项、小程序首页日历 3 项通过，Ruff、迁移漂移、OpenAPI/TypeScript 同步、全仓类型检查及管理站/小程序生产构建全绿。补充淘汰赛赛季切换竞态回归后，管理站为 45 项通过。
- `0024_manual_knockout_draws` 已完成模型与数据迁移回归并应用到本地 WSL 现有数据库：旧公开状态收敛为 `PUBLISHED`，既有淘汰赛正式双方原样回填人工映射，旧胜者关系只转换为校验来源后删除。迁移不更改既有对阵、比分、记录表、资料或调赛数据；部署后健康接口正常，公开北大杯仍为 57 队、146 场。
- ScoresheetReader 1.4 语义迁移已收口并重新部署：`0017_scoresheetreader_semantic_v2` 与纯审计回填 `0018_scoresheet_recognition_audit_backfill` 已应用；该阶段验收数字保留为历史记录，当前编辑器冻结基线已升级为提交 `f17b26e82cf3cd2d4878463e03c0580bd271fc31`。当时统一检查为 Django/PostgreSQL 135 项通过、29 项可选样本跳过，管理站 125 项、共享记录表领域 4 项、小程序 4 项通过，Ruff、迁移漂移、OpenAPI/TypeScript、全仓类型检查和两端生产构建全绿。隔离演示记录表包含双方各 12 人、19 条逐次得分、12:50 开赛与 21:18 结果；真实浏览器已验证原图/PDF/覆盖层、自动保存、服务端字段级日志、撤销恢复、0 error/3 warning 校验和单次发布确认，未执行发布。微信开发者工具可定位但截图接口返回 `SetIsBorderRequired failed`，无可用视觉树，因此真机画布、键盘和安全区仍按既有边界待人工验收。
- 小程序最终源码已通过类型检查、14 项测试和完整 Taro 微信构建；“赛程赛果 / 淘汰赛”仍是对阵页首个节点且使用 `position: sticky; top: 0`。淘汰赛新增直读正式比赛、唯一多轮键、未落位“待抽签”、签位待复核、保级赛和两类资源独立失败回归；微信开发者工具最终前台截图与横向轮次手势仍需在工具可见会话中人工收口，因此不把该项标成视觉已验证。
- 根级类型检查会先按依赖顺序构建设计变量、`@pkuba/scoresheet-domain` 和 API 客户端，再检查各 workspace；这样干净 CI 的类型检查与后续组件测试都不依赖本机残留的 `dist`。
- 超级管理员正式修改赛程的 UI 收敛到网页后台“赛程编辑”：集中展示比赛、日期、时段、场地、双方、比分、锁定与活动申请影响；男篮、女篮使用不同色系，永久不可调比赛使用独立锁定背景并保留文字状态，保存仍要求勾选核对项和二次确认。小程序仅保留赛程/资料查看及调赛处理，不暴露直接编辑入口。
- 小程序源码的完成标准固定为：TypeScript 检查通过、Taro 微信构建通过，并在微信开发者工具中编译和人工检查本轮受影响页面；工具内检查未完成时不得把前端标记为已验证。
- 本轮比赛日游标、统一比赛详情、移动管理员工作台及比赛资料展示已通过真实 WSL/PostgreSQL 全量门槛：后端 208 项通过、29 项按环境跳过，Ruff 与迁移漂移通过，OpenAPI/TypeScript 同步；共享领域 4 项、管理站 139 项、小程序 21 项通过，全仓类型检查、管理站生产构建和完整 `build:weapp` 成功。本地 `localhost:8088` 实测初始返回 5 个比赛日、向前 5 日无重复，公开赛季仍为 146 场；管理员 dashboard 返回 10 场且单次本地请求约 98 ms。当前本地赛季每场均有一张合成比赛合照和一张记录表原图，比赛详情已验证返回两队独立统计及公开合照；微信开发者工具最终页面视觉与真机网络延迟仍按本轮实际工具能力单列，不以构建结果代替。
- 比赛资料管理区已收敛为单列分类清单：记录表与比赛合照各保留一张当前图片，其他照片可多张；合照和其他照片上传后直接进入兼容的已通过状态，不再提供照片审核或手工排序。两类管理员均可重传、软删除非记录表图片；记录表永远不可删除，已发布记录表仍只允许超级管理员重传。最终统一门槛为真实 WSL/PostgreSQL 211 项通过、29 项按环境跳过，Ruff、迁移漂移、OpenAPI/TypeScript、全仓类型检查、共享领域 4 项、管理站 139 项、小程序 24 项以及两端生产构建全部通过。本地 Ubuntu 已重新部署，健康接口正常，公开北大杯仍为 57 队、146 场；只读核对 146 张当前合照分属 146 场且无重复。微信开发者工具窗口可定位，但截图接口仍返回 `SetIsBorderRequired failed`，因此本轮普通管理员、超级管理员、空资料和完整资料四种页面的最终可见样式保留为人工验收项。
- 比赛详情身份恢复已统一：所有入口使用同一个编码路由；详情页缺少或持有失效令牌时会静默执行微信身份交换，已注册管理员从公开首页直接进入也会恢复管理员角色并读取同一份媒体集合，因此与管理员工作台入口拥有完全相同的上传、重传和删除操作。未知 OpenID 保持匿名，不进入资料管理或强制注册。小程序类型检查、12 个测试文件共 29 项和完整 `build:weapp` 通过；微信开发者工具控制服务本轮连续超时，最终可见状态仍需人工补验。

### 2026-08-23 剩余功能审计

结论：V1 的核心业务链路已经具备真实页面、API、PostgreSQL 模型和自动化测试。管理站导航中的赛季、球队与名单、赛程编排/编辑、统一抽签映射、调赛、比赛资料、管理员、高级数据和备份归档均已接入；淘汰赛各轮签位统一在抽签映射中维护，不再设置独立淘汰赛管理入口。小程序的首页、对阵、排名、数据、我的、身份、领队/管理员工作台、任务箱、调赛、比赛资料和记录表也均已接入。后续不再把这些项目列为“未实现”，而按以下三类管理。

#### A. 代码尚未实现，属于上线阻断项

1. **同机蓝绿正式部署**：现有代码只是禁用的脚手架。尚需先在隔离环境完成单 project 到稳定 gateway/data/blue/green 的基线转换，再演练候选 readiness 失败不切流、切流后 5xx 应用回切、worker 卡死、迁移不兼容及数据库/媒体同时损坏的成对恢复；完成前不得启用生产工作流。
2. **生产环境初始化与合成数据阻断**：正式环境必须使用全新数据库和私有媒体存储，通过后台录入真实当前赛季；部署流程尚未强制运行 `check_no_synthetic_public_data`、最后一个超级管理员检查和公开赛季唯一性检查。
3. **普通管理员网页入口**：比赛资料与记录表入口、只读调赛列表已开放；调赛决定、赛程纠错、账号和赛季危险操作继续由超级管理员执行。该项转为回归验收，不再列为未实现。
4. **已确认规则收口**：`p4`/`p7` 默认容量为 0、V3.3/`3.3.0`、首页 35 天热图、全系统备份短暂写入围栏、另一名超级管理员安全降级和归档 publication 受控纠错已进入代码与 PostgreSQL 测试。

#### B. 代码已实现，但尚未完成真实外部验收

1. **微信视觉与真机**：TypeScript、Taro 构建和组件测试已通过，但首页热图、对阵页固定切换栏、任务红点、榜单分页、记录表画布、软键盘、安全区、前后台恢复与跨端只读切换仍需在微信开发者工具和至少一台真机人工验收。自动截图接口仍不兼容，因此不能把“构建通过”写成“视觉已验收”。
2. **Qwen 真实识别**：队列、四次尝试、30 秒重试、`d631601` 原生大图/8 MP 小图放大/20 MB Data URI 处理、180 秒 SDK 网络读写超时、长调用租约和人工兜底均已实现。2026-08-23 已使用演示纸面 JPEG 通过 `qwen3.8-max` 连续两次在第 1/4 次完成真实识别（分别总计 19,810 与 18,967 tokens），最终 Playwright 场景绿色通过，网页端正确载入比赛序号与重传审计；真实高清比赛照片、可重试失败和四次耗尽仍需单独验收。
3. **邮件闭环**：业务 Outbox、固定公邮、去重、重试和耗尽告警均已实现；尚未轮换旧 163 授权码、在 Mailpit 核对实际正文/重试并显式启用真实发送进程。所有邮件仍只发 `pkubaoutward@163.com`。
4. **正式微信和域名**：本地真实 `code2session` 已可用；生产域名、HTTPS、备案、微信 request/uploadFile/downloadFile 合法域名和正式环境登录尚未验收。
5. **备份与归档外部闭环**：归档模型、worker、三类包、Range 下载、24 小时清理、照片永久清理及 `pg_dump → 空库恢复 → 全表计数/迁移/媒体哈希 → 健康接口` WSL 演练已完成；仍需在真实管理站登录态下载一个包，并确认阿里云每日备份同时覆盖 PostgreSQL 与私有媒体卷后完成云备隔离恢复。

#### C. 已明确不做，不属于遗留功能

- 不自动按球队数量生成小组、淘汰赛阶段或应有对阵；管理员继续在现有页面逐项手工设置。
- 数据页本阶段不增加球队/球员详情页；球队展示完整榜单，球员每页 20 人、最多 5 页且只展示前 100 名。
- 不接入腾讯 COS；生产照片继续保存在 Ubuntu 私有 Docker volume。阿里云日备负责灾难恢复，管理站人工包负责赛季交接和核心开发者按需原始备份。
- V1 不实现裁判身份与排班、公众历史赛季、实时临场计分、离线权威副本、微信订阅消息、特殊调赛附件、系统内抽签、跨组排名、高级技术统计、Redis/Celery、微服务或随 `main` 自动上线。

### 下一阶段实施计划

#### 第一批（当前收口，P0）：备份与归档实机闭环

1. [x] 在本地 WSL 用最终镜像完成一次全系统原始备份和全新 `pkuba_restore_*` 隔离库恢复；自动核对全部表记录数、Django 迁移、媒体集合/大小/SHA-256，并以恢复库启动临时 API 做健康检查。
2. 已在真实登录态验证磁盘总览、赛季数据导出、短期下载票据、Range 和主动丢弃；仍需人工检查照片导出、原始备份密码重验、确认保存清理及已归档演示赛季的永久删除预览，不得在 2026 公开测试赛季执行照片清理。
3. 确认阿里云每日备份覆盖 PostgreSQL 数据卷和 `private-media` 卷，并在独立 Compose project 做一次云备恢复；应用不重复实现每日定时任务。
4. 全系统包在捕获 PostgreSQL 一致性快照和当时的不可变媒体文件集合时持独占写围栏；HTTP 写请求与业务 worker 持共享围栏。独占期间公开读取继续可用，新写入返回 503；数据库 dump、媒体文件集合与精确大小捕获完成后立即释放，后续 SHA-256 和压缩不延长维护窗口。

验收标准：恢复前后全部表计数一致，媒体数量、总字节数和 SHA-256 全部一致；迁移无缺失；健康检查成功；任何清单不符都必须停止且不能覆盖运行中的数据库或媒体目录。

#### 第二批（P0）：正式 Ubuntu 发布与空库赛季演练

1. [ ] 从固定标签 digest 完成同机蓝绿：预检 → writer fence → 同一 manifest 的 DB/媒体/归档备份 → 兼容迁移 → 候选 API/Web/worker 验收 → 持久事务保护的原子切流 → 兼容保留栈保留 24 小时。普通故障只按发布时持久化的 capability contract 回切应用且三类数据零恢复；数据受损才成对恢复。当前已补状态/身份/能力解析、全阶段事务日志、启动恢复和 SIGKILL/恢复失败隔离 command-spy，仍未完成基线转换与真实服务器演练。
2. 在全新生产候选数据库中手工走通：创建赛季 → 配置组别/场地/时段/容量/多轮签位 → 导入真实球队名单 → 编排赛程 → 公开赛季 → 小组整组映射 → 淘汰赛逐场映射。该验收不新增自动赛制向导或赛季开始开关。
3. 发布门禁必须包含：无合成数据、无活动异常流程、唯一公开赛季、至少一个有效超级管理员、备份可恢复、生产配置安全检查。
4. 完成域名、HTTPS、备案、最小权限、防火墙和微信合法域名，再做生产微信登录、上传下载、调赛、任务箱和记录表真机冒烟。

#### 第三批（P1）：外部服务、接口与依赖收口

1. 完成真实 Qwen、Mailpit、163 公邮及微信真机的验收矩阵，保留脱敏日志和截图；邮件发送进程只在凭据轮换和 Mailpit 验收后启用。
2. 对其余写接口做风险分级。现有七个不可重复关键命令已支持 `Idempotency-Key`；将调赛响应/撤回/管理员决定、资料替换/删除、赛果纠错等高风险命令纳入同一机制，纯幂等状态标记和带版本冲突保护的低风险编辑可保留现状并记录理由。
3. 对仍可能随赛季增长的管理列表补齐服务端分页；固定规模的赛季配置、组别、场地、时段和签位不为形式统一而分页。
4. 重新执行生产依赖安全审计，优先评估 Taro 升级；不得直接运行破坏性 `npm audit fix --force`。升级后必须重新完成小程序构建、开发者工具编译和真机回归。

当前仍处于测试阶段，不进入服务器实机接入，不配置生产 Secrets，也不触发生产部署。下一步先在本机隔离 project 完成蓝绿基线转换和失败演练；只有独立报告改为 GO 且用户明确要求上线后，才处理服务器。

### 当前风险

- 已新增 `docs/USER_GUIDE.md`，集中登记页面显示、登录/会话期限、权限、调赛、容量、识别重试、邮件 Outbox、幂等和归档等显式/隐式规则。管理员已确认无限身份失败重试、四字符密码下限、普通管理员入口、0 场球队、决赛容量、V3.3、仅他人可执行的超级管理员降级、按完整日期累加首页摘要、全系统备份写入维护窗口和关闭浏览器结束网页会话。
- 网页密码和小程序邀请码不再按失败次数锁定；管理员新密码只要求至少 4 个字符，可与当前密码相同，不做相似度、常见密码或纯数字限制，也不强制首次登录改密或轮换默认邀请码。该实现符合当前产品决定，但持续尝试与弱密码会增加公网风险；正式上线前必须启用 HTTPS，并建立不自动锁号的匿名失败审计告警。
- 赛程服务端、管理站和产品文档统一只接受并展示 V3.3（格式 `3.3.0`）；管理员必须从当前赛季页面下载模板，仓库示例只用于当前协议自动回归。
- 新赛季 `p4` 18:30 与 `p7` 20:30 决赛时段的工作日/周末默认容量均为 0，只能通过管理员设置的特殊日期容量开放；既有迁移历史值保留为迁移证据，不回写历史迁移。
- 网页扫码和密码登录已改为浏览器会话 Cookie，并补充两种入口的回归；正常关闭浏览器后需要重新登录，服务端 12 小时仅作为最长后备寿命。浏览器可能通过“恢复上次会话”恢复 Cookie，共享电脑仍必须主动退出。
- JavaScript 依赖使用锁文件固定，CI 必须同时通过生产依赖审计与完整 `npm ls`。critical/high 不允许豁免；moderate 只有在仓库清单逐条记录范围、理由和到期条件后才可暂时接受。Taro 或底层构建链升级后必须重新完成类型检查、组件测试、管理站构建、小程序完整构建、开发者工具和真机回归；禁止直接执行破坏性的 `npm audit fix --force`。
- 本地 `WECHAT_APP_SECRET` 已安全同步到 WSL Django 容器，真实 `code2session`、微信身份恢复和小程序会话已经由“九毛”账号成功使用；2026-08-22 只读核对仍有有效会话。生产域名、生产 AppSecret 注入、微信 request 合法域名和正式环境真机登录尚未验收，因此当前本地成功不能代替 M5 发布验收。
- 本机微信开发者工具的 CLI 服务端口当前关闭；默认启动仍会打开工具，需手动导入 `apps/miniapp`。只有用户自行在工具安全设置中开启服务端口后，才使用 `-UseWechatCli` 自动导入。
- WSL 配置只用于本机测试；其部署脚本不再自动导入旧数据、生成演示赛季或重置管理员。生产服务器的一次性受限账号/Secrets 接入、自动回滚实机演练、HTTPS、阿里云备份覆盖、域名和微信真机验收仍属 M5。

### 2026-08-25 独立复测接手状态

独立复测以提交 `04009bbad53cf2440974f76ece1ae5466cb74456` 为基线，当前总体结论仍为 **NO-GO**。本轮已在工作区收口 readiness 的迁移/存储/同版本 worker 真检查、首页固定 35 天、无比分不显示 `VS/vs`、四类配置拖拽与连续顺序、组别 code 服务端生成、识别来源字段级写保护，以及记录表/比赛资料队列的服务端筛选与分页。代码、领域/组件测试和生产构建已通过；登录态管理站、微信开发者工具和真机仍需按真实页面逐项复测，不能以构建替代。

本地 WSL 正式入口已改为先启动 PostgreSQL、等待就绪并应用迁移，再启动依赖迁移表和 worker 心跳的 API/worker/Web，消除了空库首启的循环 readiness。部署入口不执行 legacy 导入、seed、管理员重置或卷清理；当前 QA 赛季的既有验收记录保持不变。

当前明确不改：已接受风险、固定邀请码、仅超级管理员处理调赛决定、合并加时 `period=5`，以及 migrations、AdminAuditLog、publication/revision 和有效来源证据。当前明确不得执行：Qwen 新调用、生产 SSH/部署、QA 数据清理或覆盖独立测试报告。

高风险项当前决定：legacy/demo/本地初始化链在测试阶段保留，上线门禁前彻底删除；归档赛季已发布记录表受控纠错和另一名超级管理员安全降级已经实现。跨赛季边界已按用户确认落实为数据库约束：赛季专属的组别、球队、名单、赛程、签位、调赛、记录表、统计、媒体、导入和归档引用不得跨季组合；同名对象在各赛季分别创建，只有账号等全局身份可跨赛季使用。复合外键不再只拦截迁移后的新写入：后续迁移显式验证全部既有行，部署预检、迁移后验收、全系统恢复和成对恢复均运行十类只读赛季一致性审计，任一违规在备份、启动服务或切流前失败。正式记录表 Playwright 已在本地真实 API 与临时超级管理员会话通过；真实 Qwen 重传因额度和授权边界跳过，私有双标签场景仍由既有动态证据覆盖。空业务库首启、微信开发者工具逐控件、真机、蓝绿回切与数据受损成对恢复仍是上线阻断项。

本轮已在 QA 数据维护前生成同一时间点的 PostgreSQL、媒体和归档成对备份，并在隔离 PostgreSQL/卷中核对 57 张业务表计数、533 个媒体文件 SHA、迁移集合和跨赛季审计均一致。随后仅按已确认规则维护 2025/2026/2027 的标准时段与容量，保留赛季中段、用户、会话、媒体、记录表和赛果；一次性维护脚本不进入 Git。该本地证据不代替生产蓝绿或云备恢复演练。

Python 图片/PDF 依赖已升级到当前安全基线，锁文件审计为 0 个已知漏洞；CI 固定运行 `pip-audit`。这只证明锁定依赖的已知漏洞门槛，不代替运行时、前端依赖、微信真机和业务安全验收。

本轮最终本地门槛：真实 WSL/PostgreSQL 后端 294 项通过、29 项按环境跳过，Ruff、迁移漂移、OpenAPI/TypeScript、根类型检查、管理站 161 项、小程序 45 项及其产物完整性 4 项、共享领域 9 项、两端生产构建全部通过；PDF 渲染专项在 `pypdf` 新版本下无弃用警告。`npm audit --omit=dev` 无 critical/high，现有 7 项 moderate 均为已登记且有到期时间的本地 Taro/webpack 开发链依赖。正式记录表 Playwright 为 5 项通过、1 项按 Qwen 授权跳过：它会对当前真实草稿执行校验，有效草稿允许发布，含人工/识别错误的草稿验证发布阻断，同时核对既有正式 PDF 可导出。测试账号已停用并设置不可用密码；本地 8088 的运行态复核继续作为独立步骤，不以本轮构建结果代替。该证据仍需独立测试任务复核后才能改变总体 NO-GO 结论。

领队调赛已明确拆成“普通调赛”和“跨周调赛”两个入口。普通入口只返回同一自然周目标；跨周入口返回全部合法目标，因此允许与普通入口重叠。服务端另行计算同周/跨周日期关系，入口只决定普通流程或参赛手册审核通道；跨周入口首屏明确提示可选择本周或跨周时段，并说明是否属于手册意义上的跨轮次由管理员审核决定。管理员认定未跨轮次时直接按普通办法批准，无需领队重新提交。

本批调赛与发布完整性门槛在独立 PostgreSQL 17 中完成：0038→0039 迁移、值域/组合约束、历史投票证据、旧流程整事务 fail-closed、幂等与超管并发等 63 项聚焦回归通过；后端全量收集 362 项并以退出码 0 完成。Ruff、迁移漂移、OpenAPI/生成客户端与根类型检查通过；共享领域 9 项、管理站 164 项、小程序 47 项和微信产物 4 项通过，管理站生产构建与显式本地 URL 的完整 `build:weapp` 成功。发布隔离 command-spy 和蓝绿结构验收再次通过；微信开发者工具唯一 PKUBA 窗口仍因 `SetIsBorderRequired failed (0x80004002)` 无法自动截图或读取可见树，真实页面与真机继续交独立测试/人工验收，不以构建代替。

小程序构建链已加入受控缓存失效：每次正式构建先删除项目内 `production-weapp` webpack 缓存，watch 首次构建同样走完整门槛，随后仅清理 `development-weapp` 后进入增量监听。使用独立复测保留的真实空模块缓存连续执行两次相同构建均自愈，`pages/admin/index.js` 均为 4332 字节且不含空模块；watch 初始构建和 44 个入口增量编译也通过。缺少正式 API 地址时构建仍按预期失败，并保持既有 `dist` 的 SHA-256 不变。

迁移 `0038_validate_season_scope_foreign_keys` 已在当前 QA PostgreSQL 上先后执行十类跨赛季审计（全部为零）、验证 23 个赛季复合外键并再次审计（仍全部为零）；数据库查询确认 23/23 均为 `convalidated=true`。部署预检、迁移后验收、全系统恢复及成对恢复均调用同一只读审计。成对恢复清单现已覆盖 `previous-release.env`，恢复脚本不再 `source` 备份内容，而是只接受固定键和值格式；结构验收已动态篡改该文件并确认在接触数据前同时被 SHA 校验和安全解析拒绝。

## 技术架构

- 后端：Python 3.13、Django 5.2 LTS、Django Ninja、PostgreSQL。
- 小程序：Taro 4.2.1、React 18.3、TypeScript，沿用旧 AppID。
- 管理网站：React 18、TypeScript、Vite。
- 单仓库：`apps/api`、`apps/admin-web`、`apps/miniapp`、`packages/*`。
- API：统一 `/api/v1`；OpenAPI 自动生成 TypeScript 客户端。
- 本地验收：Ubuntu WSL 内用独立 Compose 运行 PostgreSQL、Django/Gunicorn、Caddy 和 Mailpit；Windows 运行微信开发者工具并通过端口代理访问。
- 快速开发：保留 Docker Desktop + Windows 前端监听脚本作为可选路径，不作为最终本地验收基线。
- 生产：单台 Linux 服务器运行 Caddy、Django、记录表/归档 worker、任务进程和 PostgreSQL；文件长期使用私有 Docker volume，不接入腾讯 COS。
- 暂不采用 Redis、Celery、微服务、Kubernetes 或客户端权威业务规则。

## 赛季、组织与公开状态

- `Season` 状态固定为 `SETUP`、`PUBLISHED`、`ARCHIVED`；同一时间至多一个 `PUBLISHED` 赛季。
- 同时最多一个公开赛季；公众不查询历史赛季，管理员可只读查看和导出归档。
- 新赛季重新创建球队、名单、赛程、签位和领队绑定；旧赛季记录保留在同一数据库并按 `season_id` 隔离。
- 北大杯默认男甲、男乙、女甲、女乙；新生杯默认男篮、女篮；准备阶段可配置。
- 只有超级管理员可以创建赛季或修改基础配置。新赛季可以使用赛事类型默认配置，也可以从任一历史赛季沿用组别、标准场地、容量和签位族；8 个稳定时段使用当前系统默认值。复制绝不带入球队、比赛、身份、比分、草稿或其他业务数据。
- 名称、赛事类型、年份、日期范围、组别、场地、时段、工作日/周末容量、特殊日期覆盖和签位族由同一带版本号的服务端事务保存；赛程草稿及其动态列使用独立版本和审计。`SETUP` 可直接维护；公开或归档后仍允许超级管理员通过影响预览、二次确认和完整审计进入维护模式，普通管理员始终只读。
- 容量只保存周中/周末默认值和按日期/时段的稀疏例外，不保存可失真的已用计数。每日台账实时汇总非 `VOID` 比赛与 `ACTIVE` 预留；容量不被场地数硬截断，也允许降到当前占用以下，既有比赛不自动取消，但必须标记 `over_capacity` 并阻止普通流程继续占用。被业务数据引用的组别、场地和时段不得无影响预览地删除。
- 赛季可以在部分签位尚未解析时公开；每场比赛只有双方均已解析且满足对应业务条件时，才允许调赛、上传资料、维护记录表或产生赛果。
- 赛季归档后领队绑定失效，所有业务数据只读。

## 身份与权限

- 普通访客无需登录即可查看当前赛季公开信息。
- 微信仅用于取得稳定 OpenID；用户首次执行身份操作时自行设置唯一昵称。系统不保存姓名、邮箱、手机号或额外显示名，昵称也是管理员网页登录名。
- 领队用同一微信账号认领未被认领的当季球队；一人一队、一队一领队，认领后只能由超级管理员审计纠正。
- 管理员在小程序使用当前赛季邀请码注册为普通管理员，并在注册表单中自行设置个人网页登录密码和再次确认。注册密码及后续新密码都只要求至少 4 个字符，可以与邀请码或当前密码相同，不做其他强度限制或首次改密要求。邀请码仅保存摘要、可随时轮换并审计，轮换不改变已有密码；已注册管理员是全局身份，不因换季失效。
- 领队绑定与管理员角色相互独立，同一账号可以同时拥有两种身份；后续进入小程序时按 OpenID 自动恢复全部身份。
- 管理网站默认使用小程序扫码确认，个人用户名/密码作为备用方式。扫码挑战五分钟失效并绑定发起浏览器；二维码令牌、浏览器令牌相互分离，只有已登录且当前仍有效的管理员可确认，确认后只能由原浏览器消费一次。
- 超级管理员可升级普通管理员，也可把其他超级管理员降级为普通管理员；降级只允许由另一名超级管理员执行，禁止自降级。升级、降级和停用都经过二次确认、版本校验和审计，且不得降级或停用最后一个有效超级管理员。
- 普通管理员与超级管理员均可上传比赛资料；首次发布前均可上传/重传记录表、编辑、完整手工录入、在四次识别耗尽后发起新周期、校验和首次发布。活动识别不能停止或重复启动；已发布记录表的重传、纠错、重新发布与强制租约，以及调赛审核/取消、赛季、球队、名单、赛程、账号、弃权和危险纠错仍仅超级管理员可操作。两类管理员均可重传或软删除合照/其他照片；记录表不可删除。

## 赛程导入、编辑与抽签

### 球队与名单

- 新赛季创建后的第一项数据工作是名单导入：下载按实际 `Division` 动态生成的模板，填写后上传审计，再一次性确认整季球队与名单。模板固定含“填写说明”页和每组一页，数据列为“球队标准名称*、球员姓名*、球衣号码（选填）”；数据页不放示例行。
- `Team.name` 是当季标准名称。首次出现的新球队直接创建；不引入独立简称，`short_name` 留空且公开接口回退显示标准名称。同一球队后续始终引用稳定 UUID，重命名不会断开比赛、抽签、排名或领队关系。
- 球队名和球员名必填；球衣号码为文本，只接受 `0–99`、`00` 或空。同队启用球员的非空号码重复属于错误并阻止确认；同队重复球员名同样阻止确认。近似名称只建议，不自动合并；管理员必须统一名称或明确确认是不同球队。
- 首次确认仅允许 `SETUP`、无已确认名单批次且不存在实际小组、签位、抽签、比赛、领队绑定或调赛等下游数据。事务可替换尚未被引用的旧球队和旧名单；任何保护引用、版本变化或创建失败都回滚整批。确认后不提供名单重置或再次导入。
- 在线维护允许新增球队和球员；已公开赛季的球队重命名/停用及既有球员修改/停用先返回维护预览并二次确认。被比赛、抽签、领队或调赛引用的球队不能停用；归档赛季只读。所有写入使用球队/球员版本、稳定 ID、事务和追加审计。

- 准备中的赛季必须先有 `Season`、`Division`、`Team`、`Period`、`PeriodCapacity` 和 `ScheduleSlotFamily`；确认只创建 `CompetitionGroup`、`ParticipantSlot` 和 `Game`，不创建/修改球队或场地，也不写 `DrawAssignment`。
- 系统生成无签名 XLSX V3.3（格式 `3.3.0`），固定包含“填写说明”“签位定义”“赛程网格”三个可见工作表。签位族由数据库中的组别、阶段、大小写敏感字母和签位数定义；第二页仅展示该配置，不读取其业务内容。工作簿不使用隐藏赛季身份、HMAC 元数据、宏、外部链接或单元格公式；不兼容旧赛季工作簿。
- 赛程网格 A 列为日期、B 列为星期，每一行是一整天；C 列起的时间、自由文本场地、顺序和“仅决赛”可直接编辑。时间必须精确命中赛季已有时段，列必须连续且组合唯一；日期、星期和可填网格均有明确横纵边框。单元格填写 `A1vsA2`，女子比赛追加 `（女）`。
- `ScheduleSlotFamily` 显式保存 `round_number`：同组别允许任意多轮 `KNOCKOUT`，半决赛、决赛和保级赛默认第 1 轮；XLSX V3.3 签位说明、赛程解析和 `Game.round_number` 使用同一字段。每个小组赛/循环赛签位族必须覆盖全部两两对阵；每一阶段轮次内每个签位必须且只能出现一次。多批新增只能增加完整阶段或签位族，未出现在新文件中的既有比赛保持不变。
- XLSX 上传先做文件和结构预检，再整表替换同一服务器草稿；非空草稿在页面显式确认，结构错误不产生修改。业务核对覆盖缺失/反向重复对阵、阶段一致性、比赛数量、日期、动态列头、场地占用、容量、同一签位同时参赛、文件安全和并发变化。
- 确认时重新锁定赛季及相关排期资源并复核上传文件 SHA-256，在单一 PostgreSQL 事务中新增记录、写入来源批次、增加赛季版本并记录审计；错误、警告之外的任何创建失败都完整回滚。
- XLSX 不包含 `leader_adjustable`，也不按阶段或文字推断；上传格默认可调。在线草稿可对已填比赛逐场切换“领队可调/不可调”，确认时按草稿值创建，正式赛程仍可在“赛程编辑”继续修改。
- 超级管理员可在 `SETUP` 状态先执行重置预检，再输入赛季名称并二次确认。重置仅删除已确认导入批次创建且仍安全的比赛、签位和小组；任何抽签、赛果、申请、预留、媒体或非导入引用都会阻止整次操作，手工对象始终保留。
- 在线编辑与 XLSX 共用同一日期×时段/场地校验；草稿自动保存采用版本检查和追加审计，确认前不写正式赛程。
- 抽签页面统一展示 `group` 非空的小组赛/循环赛初始签位，以及所有淘汰赛、半决赛、决赛和保级赛阶段轮次。球队选项只来自当前组别的启用 `Team`，不允许手工输入、跨组选择或停用球队。
- 小组初始映射每次提交一个组别的完整签位快照，并要求启用球队数与初始签位数一致。淘汰赛双方完整后按比赛逐场预览、逐场保存；第一轮可选本组全部启用球队，后续轮次优先列出紧邻上一轮正式胜队，其他选择生成必须再次确认的警告；保级赛只校验同组、启用和同轮不重复。
- `DrawAssignment` 允许同一球队在不同轮次重复，保留签位唯一；服务端事务锁定当前轮次全部比赛和映射以保证同轮不重复。映射可保存来源比赛、来源版本及 `NOT_APPLICABLE / WINNER_CONFIRMED / SUPERADMIN_OVERRIDE / LEGACY_IMPORTED` 校验模式，来源只用于审计和动态复核，不驱动任何写入。
- `SETUP` 和 `PUBLISHED` 均可落位；保存前展示变更、警告、阻塞和公开影响。真正更换已有赛果、调赛、记录表或资料比赛的参赛方仍由危险修改保护阻止；`ARCHIVED` 只读。带签位比赛不能从通用赛程编辑器直接改球队，统一引导到抽签映射。

## 调赛不变量

- `Game.leader_adjustable`、`active_reschedule_request_id` 和 `version` 必须分离。
- 截止日取原比赛与目标比赛日期较早者：提交为 D-3 24:00，球队确认为 D-2 24:00；周一至周日为同一周。
- 提交事务同时复核权限、关系、策略、截止、版本与容量，选择首个可用场地，创建申请，锁定原比赛并预留目标场地。
- 服务端可以预先决定并锁定具体目标场地，但申请生效前任何领队或管理员业务接口、页面、任务与邮件都只能显示日期和时段；通过后只以正式赛程中的场地为公开权威。
- 状态至少包含：等待对手、等待管理员决定、等待指定球队、等待管理员终审、通过、拒绝、撤回、过期、管理员取消。
- 只有普通流程且日期关系为同周的申请在对手按时同意后自动生效；参赛手册审核通道（包括同周目标）及任何跨周申请都先等待管理员分类。认定为跨轮次后可直批或指定球队确认，球队按时确认后可无限期等待管理员终审。
- 指定球队候选由服务端权威计算：有小组归属时限定同小组；淘汰赛等 `Game.group=NULL` 时限定同赛事组别内全部启用球队；两者均排除比赛双方，并由超级管理员精确勾选。
- 所有非终态持续持有原比赛锁和目标预留。通过时原子转换；拒绝、撤回、超时或取消时幂等释放。
- 标准容量同时统计正式比赛和有效预留，并保证同一场地唯一。
- 北大杯默认 8 个时段：12:50、14:20、15:50、18:30、18:20、19:50、20:30、20:40；工作日/周末容量分别为 1/3、0/3、0/3、0/0、0/2、0/2、0/0、1/0，并可由特殊日期稀疏覆盖。
- 超级管理员例外不修改容量元信息，不静默损害无关申请，必须二次确认并完整审计。
- 特殊原因调赛及协商失败抽签继续在线下处理；抽签申请必须按照《参赛手册》规定的条件、时限和完整步骤办理，包括按手册要求发送邮件、提交说明或材料，具体要求以参赛手册为准。系统只录入最终结果。

## 排名

- 排名由 Django 根据 PostgreSQL 中当前公开赛季的正式小组赛比分计算，客户端不保存或推断权威顺序。
- 正常比赛胜者 2 分、负者 1 分；弃权胜者 2 分、负者 0 分。未录入比分、未解析参赛方或已作废比赛不计入排名。
- 先比较总积分；同分依次比较相互积分、相互净胜分、相互总得分、总净胜分和总得分。所有正式指标仍相同则显示并列名次，不做跨组排名。
- 小程序按男甲、男乙、女甲、女乙及小组切换，展示排名、赛/胜/负/积分/净胜和交手比分矩阵。

## 公开数据榜单

- 球队榜只使用当前公开赛季中 `COMPLETED` 和 `FORFEIT` 且比分完整的正式比赛；小程序支持按场均得分、总得分、场均失分、场均净胜、胜率和胜场排序，不提供场次排序，但仍展示场次。
- 球员榜只使用正常完赛比赛的当前 `ScoresheetPublication` 与稳定 `RosterPlayer`；小程序支持按场均/总得分、1/2/3 分事件及犯规排序，不提供出场或首发次数排序，但仍展示相关数值。弃权不伪造球员数据，未出场球员保留在名单但不进入榜单。
- 公开 API 分页返回榜单和已发布场次摘要，单场完整统计仅在打开对应比赛后读取；客户端不得从旧 publication、草稿或非正式字段自行累计。
- `seed_public_leaderboard_demo` 默认 dry-run，只有显式确认、有效超级管理员且目标公开赛季没有既有名单/记录表时才运行；中途失败原子回滚并清理新文件。既有未建草稿的记录表来源只复用、不删除或替换。

## 淘汰赛对阵

- 公开对阵由 Django 直接读取当前公开赛季的正式 `Game`、比分和人工签位校验状态；客户端不得依赖旧小程序硬编码、来源关系或上一轮赛果临时推导球队、胜者或冠军。
- 小程序“对阵”页顶部固定“赛程赛果 / 淘汰赛”切换栏。赛程与淘汰赛分别加载、分别报错；任一接口失败不隐藏另一视图。
- 淘汰赛按组别和 `(stage, round_number)` 唯一键展示可横向吸附的轮次列，不绘制暗示固定晋级路线的连接线。每场显示日期、时间、场地、正式双方、比分和正式胜者；未落位一律显示“待抽签”，保级赛单独列出。
- 赛果纠正造成上一轮胜队集合变化时，后续人工签位、比分、记录表、资料和调赛数据全部保持不变；动态校验把不一致的球队、比赛、轮次以及相关冠军标为“签位待复核”。超级管理员可按原球队重新确认或显式越过警告，归档赛季不可修改。

## 记录表、赛果与媒体

> **范围边界：** 旧小程序和 `ScoresheetReader` 保持只读。电脑端以 `ScoresheetReader` 提交 `f17b26e82cf3cd2d4878463e03c0580bd271fc31` 为冻结基线，完整迁移原界面、编辑方式、领域模型、固定累计分格派生语义、FIBA 2024 校验、单页 A4 模板坐标与渲染；识别图片准备与 Qwen 请求模式以提交 `d631601` 为基线。只替换 PKUBA 认证、数据、租约和同步适配。小程序按手机操作设计无损适配；PostgreSQL 是唯一业务权威。

- 图片以 `GameMediaAsset` 按稳定 game ID 隔离，不通过公开静态路径暴露。媒体类别固定为 `SCORESHEET`、`GROUP_PHOTO` 和 `GAME_PHOTO`；记录表与合照各只有一张当前图片，其他照片可多张。非记录表图片不再审核或手工排序，普通管理员与超级管理员均可重传和软删除；记录表删除统一拒绝，所有变更留审计。上传先写数据库持久化暂存记录，再按块写入临时文件并计算真实字节与 SHA-256，校验成功后原子晋升正式资产；不设置应用层字节上限，Pillow 解压炸弹保护仍作为 413 安全边界。
- 一场比赛只有一张当前记录表来源；普通管理员或超级管理员上传后冻结比赛与双方名单先验并创建唯一服务端草稿。首次发布前两类管理员均可重传、编辑、完整手工录入、在四次识别耗尽后发起新周期、校验并发布；活动识别期间任何人都不能停止或重复启动。强制接管以及已发布纠错、重传和重新发布仅超级管理员。
- Qwen 一次识别周期最多四次调用，三次自动重试的默认等待均为 30 秒；服务商给出更长 `Retry-After` 时优先。网络、180 秒 SDK 读写超时、429、5xx、无效 JSON 和 Schema 错误可重试。8 MP 只用于小图放大；大图不主动降采样，完整 Data URI 超限时保持尺寸并二分选择最高可用品质，质量 1 仍超限则不调用 Qwen且不可重试。模型只收到处理后的完整图片、球队名和球员姓名。
- 网页和小程序共用草稿版本、事件序列、校验报告、识别任务和 publication；两秒轮询，不引入 WebSocket。60 秒单编辑租约按 15 秒心跳续期，两端自动取得/释放并在失效后自动恢复，不暴露手工接手、交接或强制接管按钮。
- 网页完整保留冻结版 `ScoresheetReader` 全视口工作台和原有功能，并接入 750 ms 自动保存；小程序直接编辑与网页相同的 canonical v1.4 草稿，使用原生分区步骤、原图/标准表单视图和四块纸面累计得分格，不使用 `web-view`，不做实时临场计分，也不提供四角对齐或移动端投影层。页面采用固定视口 Flex，标准表和逐次得分各只有一个纵向滚动面；暂停、球员、犯规及选择器操作即时进入共享草稿，文本输入按 1 秒合并保存。两端始终提供“记录台人员 · 不分岗位”姓名列表，无识别结果时也可人工增删；该列表不等同于已应用识别结果，明确岗位人员仍另行填写。
- 六个区域只用于小程序导航、问题定位和识别差异，不设人工逐区确认门槛。发布前服务端 error 必须为零；全部 warning 只做一次确认，并要求所有摘要和租约仍有效且比分不为平局。
- 发布事务一次创建不可变 publication、球队/球员单场统计，更新正式比分/比赛状态/来源审核并写完整审计；任一步失败全部回滚。弃权继续走超级管理员现有 20:0 流程。
- 领队只能查看本队比赛当前 publication 绑定的记录表原图；公众可看当前 publication 的纸面统计及在线未删除的比赛合照，不能访问记录表和其他私有照片。纠错期间旧 publication 和旧记录表原图继续对外，直至新版本发布成功。
- 公开统计不提供命中率、篮板、助攻、抢断、盖帽、失误、上场时间或正负值。管理员可导出标准记录表 PDF、单场 CSV 和赛季 XLSX。
- 私有文件长期保存在 Docker volume；赛季导出、全系统备份、照片清理和隔离恢复要求见 `docs/BACKUP_AND_ARCHIVE.md` 与 `docs/DEPLOYMENT.md`。

## 产品界面

小程序底部入口：首页、对阵、排名、数据、我的；自定义底栏使用 30rpx 加粗文字并由每个页面主动同步当前项。“我的”显示 1–99/99+ 的待处理任务红点，打开任务只记录已查看，只有业务进入终态才减少数字。首页首个数据模块是只读赛季热图。“对阵”同时承载赛程赛果与多轮淘汰赛对阵，其切换栏始终固定在页面顶部；点击比赛进入私有资料页。“我的”静默恢复已注册微信身份，并统一承载任务箱、两级球队认领、管理员注册及角色工作台入口。

视觉采用白色/暖灰底、深色正文和单一 PKUBA 红；小程序首页使用由旧项目原始 Logo 确定性生成的透明资产。比赛信息优先，避免渐变、重阴影、无意义卡片及解释界面自身的冗余说明文字。

## 实施顺序

1. 文档、GPL-3.0、单仓库、开发脚本与 CI。
2. PostgreSQL、核心 Django 模型、样例数据和公开赛程 API。
3. 管理站与小程序读取真实 API 的纵向主干。
4. 赛季/球队/名单、XLSX、在线网格、抽签与归档。
5. 微信身份、管理员、领队和权限。
6. 调赛事务、预留、任务箱和邮件 Outbox。
7. 记录表 PostgreSQL worker、冻结版 `ScoresheetReader` 电脑端迁移、小程序无损适配、跨端编辑、校验发布、统计和导出；旧仓库保持只读。
8. 安全、本地卷备份恢复、赛季照片归档、域名与手动版本发布。

## 交付和发布

- GitHub 私有仓库按 2–4 人团队协作：短生命周期分支、Pull Request、至少一名非作者审核和 squash merge。完整流程以 `WORKFLOW.md` 为唯一权威。
- 当前测试阶段尚未启用 GitHub 分支保护；正式上线前必须把 `main` 配置为只允许 PR，启用 stale approval 失效、会话解决、`backend` / `frontend` / `openapi` required checks，并禁止 force-push 与删除。
- `main` 不自动上线；独立测试通过后仅由授权发布负责人创建明确版本标签，随后触发候选发布和蓝绿验收。
- 部署顺序：拉取固定 digest → writer fence → 生成同一 manifest 的 DB/媒体/归档备份 → 兼容迁移 → 隔离候选 API/Web/worker 验收 → Caddy 原子切流 → capability-compatible 旧应用保留 24 小时。普通故障只使用持久化回切合同切换应用且不恢复三类数据；只有确认数据损坏时才成对恢复数据库、媒体和归档。nullable schema 本身不构成旧应用语义兼容，当前测试阶段禁止启用该自动化。
- 阿里云每日备份必须同时覆盖 PostgreSQL 与私有媒体卷；保留策略在云平台配置，并定期在隔离环境恢复演练。应用只提供人工导出和按需原始备份，不重复调度每日任务。
- 邮件使用 PostgreSQL Outbox 和独立任务进程；唯一收件人固定为协会公邮 `pkubaoutward@163.com`，永不配置协会成员或领队邮箱。业务事务与 Outbox 入队同事务，SMTP 失败只重试并在耗尽后创建超级管理员异常任务，不回滚业务；业务正文已接线，真实发送进程默认禁用。
- 生产域名使用 `api.<domain>` 和 `admin.<domain>`。大陆服务器需在正式开放前完成备案。

## V1 不包含

- 裁判注册、登录、个人赛程或排班。
- 旧 OpenID、密码、进行中申请或旧赛季数据迁移。
- 公众历史赛季查询。
- 实时临场计分、离线权威副本和微信订阅消息。
- 特殊原因调赛附件、系统内抽签、跨组排名和高级技术统计。
- 多人实时协同编辑、Redis、Celery、微服务和自动随 `main` 上线。
