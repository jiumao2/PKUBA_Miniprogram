# PKUBA 小程序重建计划

## 目标

在不修改旧项目的前提下，以 Django/PostgreSQL 为唯一权威数据源，重建微信小程序和内部管理网站。新版必须复现已确认的旧业务，并修正调赛锁、容量预留、权限、记录表发布与审计缺陷。

旧系统审计与规则依据见 `PKUBA_现有小程序功能与逻辑规格.md`。与旧实现冲突时，本文件中的已确认产品决定优先。

## 当前状态

| 里程碑 | 状态 | 完成条件 |
|---|---|---|
| M0 文档与单仓库 | 已完成 | Git、GPL-3.0、开发脚本、README、CI |
| M1 纵向主干 | 已完成 | PostgreSQL → Django API → 管理站/小程序真实赛程 |
| M2 赛季与赛程 | 核心已实现，验收中 | 赛季/组别、球队与名单、场地/时段/容量、V3.2 赛程编排、抽签落位、分组上线/安全回退/整季归档，以及权威淘汰赛关系与级联纠错均已实现；真实登录态与微信端验收继续收口 |
| M3 身份与调赛 | 核心已实现，邮件外发待启用 | 领队/管理员身份、调赛事务、角色工作台、网页扫码/密码登录、任务箱和超级管理员赛程例外均已完成；业务事件已写入固定公邮 Outbox，真实 SMTP 外发须在轮换凭据并完成验收后启用 |
| M4 记录表与统计 | 已实现，真机待验收 | 唯一原图、Qwen 四次尝试、共享草稿、自动租约、两秒同步、原版电脑编辑器、小程序无损适配、原子发布、公开统计与 PDF/CSV/XLSX 已实现；仍需真实 Qwen 凭据冒烟和微信开发者工具/真机交互验收 |
| M5 上线 | 下一阶段 | 核心业务页面已齐备；待完成 COS、备份恢复、完整归档、正式部署、外部服务与微信真机验收 |

### 已验证实现（2026-08-21）

- 已建立 GPL-3.0 单仓库、开发/生产 Compose 骨架、PowerShell 脚本和 GitHub Actions。
- 已在真实 PostgreSQL 上完成核心迁移、演示数据、公开当前赛季及赛程 API。
- 管理网站和 Taro 小程序均通过生成的 OpenAPI 类型读取真实 API；没有用静态假数据冒充纵向切片。
- 已实现无签名 XLSX V3.2 与统一在线草稿：固定“填写说明、签位定义、赛程网格”三个可见工作表；第二页只提示数据库签位字母含义且不参与业务导入。第三页按一天一行展开日期与星期，C 列起动态识别 1–64 个连续“赛季时段 + 自由文本场地”组合，支持显式“仅决赛”标记，不含隐藏赛季身份、HMAC 或逐场可调字段。
- 已实现服务器版本化 `ScheduleGridDraft`、动态列和稀疏比赛格。管理端“赛程编排”解除普通工作区宽度上限，并提供占满视口的专注模式和可收起检查器；按住鼠标左键可框选矩形区域，批量男女篮及领队调赛锁只在所选区域包含比赛时启用。比赛移动由格内独立抓手触发，避免与框选冲突并坚持只移动、不覆盖；直接输入、TSV 复制粘贴、100 步会话撤回/重做、动态列增删排序和 700ms 自动保存继续保留。XLSX 上传整表替换草稿；结构错误不修改草稿，业务错误可在线继续修正。
- 已实现草稿显式六项核对、问题分类和完整比赛预览；确认只新增小组、签位和比赛，并在单一 PostgreSQL 事务中写来源批次、赛季版本与审计。在线逐场 `leader_adjustable` 会写入正式比赛；XLSX 上传的格默认可调，确认后清空已提交草稿格而保留列。
- 已实现准备中赛季的导入重置预检与二次确认：只删除可追溯到已确认批次且仍安全的导入对象，抽签、赛果、调赛申请/预留、媒体、非准备状态或外部引用都会阻止整个事务。
- 已实现超级管理员“赛季与组别”工作区：可新建准备中赛季，采用系统默认配置或沿用历史赛季的组别、标准场地、容量和签位族；8 个稳定时段继续用于容量归属。赛程网格列不再是基础配置，也不从历史赛季复制；新空白草稿仅用 16 个便捷组合初始化，之后由管理员在网格表头自由编辑。
- 已实现超级管理员“球队与名单”工作区：准备中赛季按实际组别生成带“填写说明”的三列表格模板，上传后检查文件安全、必填项、`00`/`0–99` 号码、重名、重号、跨组重复、近似名称和签位数差异。新球队在首次导入中直接创建，不匹配历史名称；管理员逐项确认标准名称后，以整季快照在单事务中创建稳定球队/球员 UUID、增加赛季版本并写完整审计。每季只允许确认一次，此后永久关闭名单重导并改走主从在线编辑；进行中赛季的重命名、停用和既有球员修改必须先预览影响并二次确认，归档赛季只读。
- 球队与名单实现已通过真实 PostgreSQL 全量后端 75 项、管理站全量 21 项、Ruff、迁移漂移、OpenAPI 连续生成稳定性、全仓类型检查和生产构建。只读参考工作簿回归确认可检出跨组重复、`-121` 非法号码、同队重号和近似队名，未执行确认写入。本地 WSL 已应用 `0011_roster_imports_and_jersey_numbers`；真实超级管理员浏览器验收覆盖模板下载、球队搜索/组别筛选、桌面与 390px 窄屏主从布局且无控制台错误。验收全程未上传、保存或确认，当前 ACTIVE 北大杯仍为 57 队、146 场比赛、0 名名单球员和 0 个已确认名单批次。
- 已实现超级管理员“抽签映射”工作区：按组别展示小组赛/循环赛初始签位，并从该组别启用球队数据中提供下拉选项；已选择球队实时从其他签位移除。每次必须提交一个组别的完整一对一映射，保存前预览签位变更、受影响比赛和公开影响，确认后在同一事务中更新 `DrawAssignment`、比赛双方、赛季/比赛版本和审计。进行中赛季仅允许修正尚未开赛、无赛果、无调赛历史且无比赛资料的相关比赛；归档赛季只读。
- 抽签映射已通过真实 WSL/PostgreSQL 全仓回归：后端 79 项通过、29 项按环境跳过，管理站 24 项通过，Ruff、迁移漂移、OpenAPI 生成、全仓类型检查、管理站生产构建和微信小程序构建全绿。`localhost:8088` 真实超级管理员登录态显示既有 2026 四个组别全部完成；已验证下拉去重、不完整映射禁用保存、撤销恢复、固定保存栏和零控制台错误，未提交任何业务写入。
- 新赛季默认标准场地为五四东一、五四东二、五四东三；超级管理员可增删、改名、排序或停用，至少保留一个启用场地。普通调赛只从当季启用的标准场地按顺序自动分配。邱德拔及历史自由文本地点可作为具体比赛或特殊赛程网格地点保留，不会因存在历史引用而自动进入标准候选池。
- 标准时段固定预设为 8 个稳定代码，管理员通常只需核对名称和默认时间，不再手动增删。比赛同时保存标准时段和实际开赛时间；时间微调时仍按标准时段计算容量，调赛必须同时更新目标日期、标准时段与实际时间。
- 实时容量台账不另存“每日已用数”：服务端按正式比赛与 `ACTIVE` 预留即时计算。管理总览和赛季配置页均用从首场到末场、按周一至周日排列的双周历展示每日比赛数与每日剩余容量，并联动查看同一天。
- 特殊日期容量是管理员显式维护的稀疏覆盖，新赛季默认完全为空，绝不依据实际赛程反推或自动补高。旧快照中曾自动推导的 34 条记录只保留为 `LEGACY_INFERRED` 审计证据，不参与容量计算，也不出现在管理员例外列表。
- 赛季基础配置保存时由 PostgreSQL 事务重新校验版本、代码/顺序唯一性、资源引用，以及签位族的连续性、顺序和前缀唯一性；草稿或 XLSX 导入另行检查动态网格列唯一性。容量允许高于标准场地数或低于当前占用，但预览与台账会明确返回 `over_capacity`；占用只统计非 `VOID` 比赛和 `ACTIVE` 预留，`CONVERTED` 不重复计数。所有成功修改保存完整前后审计快照。
- 已实现个人管理员密码会话、一次性登录挑战、CSRF、失败限流、后台改密、超级管理员账户列表、只升级不降级、停用/恢复、最后一个有效超级管理员保护和审计。
- 已完成网页扫码登录闭环：网页默认生成五分钟一次性二维码和六位校验码，二维码令牌只以摘要入库，浏览器另持有不进入二维码的会话绑定令牌；已登录管理员在小程序“我的”中扫码、核对并确认后，原浏览器才能单次换取 Django 会话。确认和消费分别原子加锁、重复确认幂等、跨账号确认冲突、过期/停用/重放全部拒绝并审计；个人用户名/密码继续作为备用入口。
- 已实现调赛领域事务：原比赛活动锁、目标容量/场地预留、同周自动生效、跨周投票/终审和幂等释放，并含 PostgreSQL 并发争抢测试。
- 管理网站已启用“调赛处理”：普通管理员和超级管理员均可只读查看当前公开赛季的进行中/历史申请并核对原赛程、目标预留、容量、活动锁和球队确认；直批、拒绝、指定球队投票、终审和管理员取消仅超级管理员可执行。后台使用独立会话/CSRF 接口、申请版本检查和完整前后快照审计，不能从页面手动解锁或释放预留。
- 已实现 PostgreSQL 权威任务箱：对手确认、指定球队投票、超级管理员审核/终审、记录表 AI 成功核对和识别异常均由业务事务同步创建或关闭；任务以账号和业务状态幂等键去重，阅读只写 `read_at`，不改变待处理状态。小程序“我的”及底栏显示 1–99/99+ 红点，任务列表区分待处理/已完成并使用服务端生成的业务深链接。固定公邮 Outbox 仅允许 `pkubaoutward@163.com`，调赛每个权威状态、缺少领队等调赛异常及记录表识别最终失败均与业务写入同事务排队；同一状态或异常只生成一封，历史任务重建不补发。发送进程支持行锁领取、退避重试、进程中断恢复和耗尽后超级管理员异常任务，SMTP 失败不回滚业务。
- 已将 `Idempotency-Key` 接入调赛创建、记录表发布、赛程确认、抽签确认、赛季上线和比赛资料上传：原始键不入库，同账号/命令/键的并发请求由 PostgreSQL 事务 advisory lock 串行化，首次成功结果与业务写入同事务保存 24 小时；相同请求重放原结果，不同请求复用同键返回 409。公开赛程、领队申请、管理端媒体和记录表队列统一返回单页最多 100 条的分页结构，共享客户端按页兼容既有完整列表界面。
- 任务箱与 Outbox 已部署到本地 `Ubuntu-24.04`：`0019_inbox_tasks_and_email_outbox` 已应用，公开赛季仍为 57 队、146 场，真实库保持 0 个任务和 0 封邮件；补建命令只读预览了 0 个调赛申请与 139 份既有记录表，未执行写入。统一回归为 Django/PostgreSQL 148 项通过、29 项按环境跳过，管理站 125 项、共享领域 4 项、小程序 4 项通过，Ruff、迁移漂移、OpenAPI/TypeScript、全仓类型检查和两端生产构建均通过。微信开发者工具窗口可定位且最新 `dist` 已生成，但其截图接口仍返回 `SetIsBorderRequired failed`，任务页与底栏红点的工具内视觉检查保留为人工验收项。
- 已修复重复调赛遗留的已转换场地占用：新申请通过时先锁定并释放该比赛此前的 `CONVERTED` 分配，再原子转换当前预留。标准场地和自由文本场地统一按 Unicode、空白及大小写归一化后检查冲突；名单维护预览哈希绑定完整目标球员快照；邀请码页面严格跟随后台当前选择赛季。
- 已实现组别级运营状态 `SETUP → PRE_DRAW_PUBLIC → ACTIVE → ARCHIVED`：占位符赛程按整季公开，抽签完整的组别可独立上线，第一个组别上线时全局赛季进入 `ACTIVE`；尚未上线组别仍可公开占位符，但不能调赛、录入记录表或赛果。安全回退会阻止任何已有赛果、调赛、预留、记录表或媒体的组别，归档会阻止所有活动流程且为不可逆终态。
- 已实现权威 `GameWinnerFeed` 淘汰赛链路及管理页：2026 既有对阵先以 `LEGACY_DERIVED` 只读展示，超级管理员预览并确认后才写权威关系。正式赛果在同一事务中传播胜者；赛果改变前必须预览并显式确认级联重置，自动取消受影响调赛、释放预留、清除下游参赛方/比分/当前发布指针，同时保留不可变修订、照片和审计。
- 已实现仅超级管理员可访问的“高级数据”页：静态覆盖全部 PKUBA 核心模型并显示完整数据库字段，响应禁止缓存；身份凭据、审计、发布和工作流模型只读。当前只有准备中且无引用的小组与签位可通过校验、影响预览、二次确认和完整审计进行有限物理修改，归档数据一律只读。
- 已冻结旧 `ScoresheetReader` 提交 `c27fe987a71637a8f1161a991d3c15f6c0afdeed`，把其记录表领域模型、FIBA 2024 校验、单页 A4 PDF 渲染、模板坐标和完整电脑端编辑器迁入本仓库；旧项目本身保持只读。网页与小程序共享 Django/PostgreSQL 权威草稿，小程序通过无损投影适配手机操作。
- V3.2 完整回归已在真实 WSL/PostgreSQL 环境通过：后端 86 项通过、29 项按环境跳过；管理站补充专注模式、鼠标框选、空选择禁用、抓手移动和表格独立缩放回归后为 36 项通过。Ruff、迁移漂移、OpenAPI 导出、TypeScript 客户端生成、全仓类型检查、管理站生产构建和 Taro 微信小程序构建全绿。
- 真实超级管理员登录态已验收重新构建后的 V3.2 页面：三步导航等宽显示，网格为 51 个日期、16 个可编辑便捷列和 816 个比赛格，工具栏包含复制、清空、撤回/重做、男女标记及领队调赛锁；决赛专用列与周末行配色正确，页面不再出现“比赛清单”“标准场地时段”或“可调政策”，控制台无警告或错误。既有 2026 赛季因没有签位族而按设计阻止模板下载；配置完整赛季的下载路由和组件交互已分别由 API 与组件测试覆盖。
- `0009_schedule_slots_and_daily_capacity` 已在真实 WSL/PostgreSQL 现有数据上完整提交：146 场比赛均有非空实际时间与场地文字，旧 `venue_id` 已移除，普通约束和活动/已转换预留的条件唯一索引均存在。当前既有 2026 赛季签位族为 0，仍会因缺少赛制而阻止新赛程确认；旧网格列是否为空不再影响 V3.2 模板或草稿。
- `0010_standard_venues_and_capacity_origin` 已应用到同一真实 PostgreSQL 数据库：当前赛季对外仅有 3 个标准场地、8 个预设时段、0 条管理员特殊容量；34 条旧自动推导覆盖被保留但已隔离。迁移不物理删除仍被历史比赛或特殊网格引用的地点。
- 容量与配置 UI 已在 `localhost:8088` 的真实超级管理员登录态验收：总览先显示“比赛与容量”、再显示“近期赛程”；赛季页显示标准场地、8 个稳定时段、容量与签位方案，且不再出现赛程网格列配置。V3.2 动态列只在“赛程编排”草稿或 XLSX 表头维护。
- 赛程编排 V3.2 的 2026 北大杯示例目标保持 51 个日期行、16 个便捷初始列、97 个签位和 146 场比赛；第一页规则与汇总、第三页表头/日期/可填格使用显式细实线边框并显示星期。管理端采用三步工作区和右侧“待排比赛/核对”检查器，确认前显式展示六项机器核对；任一项未通过都会禁用确认。
- 已在本机 `Ubuntu-24.04` WSL 内实际部署 PostgreSQL 17、Django/Gunicorn、Caddy 和 Mailpit；Windows 通过固定 `localhost:8088` 访问管理站和同源 API，小程序构建使用同一地址。
- 已接入微信 `code2session`、短期注册票据和摘要化小程序会话令牌；账号只保存唯一昵称，不保存姓名、邮箱或手机号，同一账号可同时拥有领队与管理员身份。
- 管理员首次注册只校验当前赛季邀请码，初始网页登录密码与该邀请码相同；后台可验证当前密码后修改，邀请码轮换不改变已有管理员密码。
- 已以只读方式从三份 `_2026北大杯` 公开备份重建“北大杯”：4 个组别、57 支球队、146 场比赛、142 场正式比分；个人身份、申请、照片和技术统计均未导入。
- 小程序 Logo 从旧页面实际引用的 `logo.jpg` 确定性提取原笔画并生成透明 PNG，所有小程序与管理站入口统一使用该资产。首页和赛程共用日期/时间分组，比赛只显示男甲、男乙、女甲或女乙，不重复标注“男篮/女篮”，仍以深灰/红色细标识增强区分。当前无未来比赛时展示近期赛果，不显示赛季结束状态。
- 小程序首页在 Logo 与赛季标题下首先展示覆盖首场至末场、按周一至周日补齐的赛季热图；零比赛日期明确显示 0，范围外补齐格弱化，非零比赛使用四档 PKUBA 红且始终保留数字，今日在范围内时显示描边与“今”。公开首页接口连续返回首末场之间的每一天并排除 `VOID` 比赛。
- 首页只承载赛事信息，不显示登录或账号摘要；所有微信登录、球队认领和管理员注册统一从“我的”进入，未登录时只显示一个微信登录入口。
- 管理后台“近期赛程”已复用同一信息层级：按日期和开赛时间分组，明确显示男女组别、场地、球队与比分；赛季结束后默认从最后一个比赛日向前展示。
- 已实现 `/api/v1/public/standings` 和小程序排名页：后端按正式小组赛比分计算 2/1 分，弃权计算 2/0 分，应用已确认的组内同分规则，并提供排名表及交手矩阵；2026 数据核对为 4 个组别、11 个小组、57 支球队和 126 场小组赛。
- 已完成小程序“数据”页三段式结构：顶部固定球队榜、球员榜和单场数据切换，按组别筛选并支持常用指标升降序。球队榜自动取齐服务端全部分页并展示所选组别的完整榜单；球员榜固定每页 20 人、最多 5 页，仅展示前 100 名。球队榜从 138 场正常完赛与 4 场弃权的正式比分计算；球员榜只聚合每场当前 publication 中绑定稳定名单 ID 的纸面字段，历史 publication 不重复累计，单场详情按需加载。
- 本地 2026 数据已写入 57 队、684 名确定性虚构名单球员和 138 份合成 publication；全部正常比赛均经过上传来源、识别结果应用、人工复核标记、服务端校验、warning 确认和正式发布服务，4 场弃权与 4 场未赛不生成球员记录。审计统一标记 `PUBLIC_LEADERBOARD_SYNTHETIC_SEEDED`，生产准备检查必须拒绝这批数据，正式上线前整批清空。
- 小程序用户可见文字统一以 26rpx 为下限，正文主要使用 28–30rpx；首页以赛季年份、醒目的“北大杯”标题和原 Logo 形成主视觉，不增加测试或赛季结束文案。
- 小程序会话支持服务端注销；本地令牌过期时会重新执行 `wx.login` 恢复已知 OpenID 的全部身份。领队认领和管理员邀请码注册继续共用同一微信账号。
- 小程序使用自定义底部导航，五个入口文字统一为 30rpx；“赛程”入口升级为“对阵”，在同一页面切换完整赛程赛果与各组别淘汰赛晋级图。“赛程赛果 / 淘汰赛”切换栏位于页面内容第一行并固定在顶部，长赛程滚动时无需返回页首。
- 球队认领改为两级选择：先选男甲/男乙/女甲/女乙，再选该组别中仍未认领的球队；提交时仍由服务端事务保证一人一队、一队一领队。
- 已实现公开淘汰赛接口和小程序横向轮次图：按真实 `Game`、比分和阶段生成四分之一决赛、半决赛、决赛及保级赛，标记胜者并展示后续晋级来源；未录入的赛果保持空白，不从旧硬编码简称伪造结果。
- 已迁移旧小程序仍有效的领队流程：本队赛程、普通/跨周调赛申请、申请列表、对手确认、指定球队投票、撤回，以及特殊原因调赛/协商失败抽签的线下说明。
- 已迁移旧小程序仍有效的管理员查看流程；调赛直批/拒绝/指定投票/终审/取消和赛程纠错统一收口为超级管理员能力，并保留二次确认、版本检查、冲突预览、活动申请取消和完整审计。记录表首次发布前普通管理员与超级管理员均可上传、重传、编辑、手工录入、失败后重新识别、校验和发布；旧裁判入口不在 V1。
- 自定义底栏由五个 Tab 页在显示时主动同步选中索引；点击不再依赖当前页面底栏实例中的旧 `selected`，避免高亮与实际页面不一致。
- “我的”在本地没有会话令牌时会静默执行 `wx.login` 与 `code2session`；已注册 OpenID 自动恢复账号、领队绑定和管理员角色，只有未知 OpenID 才进入昵称注册。
- 已实现按 game ID 隔离的私有比赛媒体，并固定分为“记录表、比赛合照、其他照片”三类；普通管理员与超级管理员可上传资料，非记录表图片审核和删除仅超级管理员。记录表首次发布前两类管理员均可重传，发布后仅超级管理员可重传或纠错。记录表每场一张当前原图，领队只能查看本队比赛当前 publication 绑定的原图；所有替换和软删除保留旧文件与审计，访问使用短期签名链接。
- 已实现 PostgreSQL 识别队列、Qwen 安全预处理、首次加三次自动重试、服务商更长 `Retry-After`、重传重置和迟到结果隔离；三段默认等待均为 30 秒。活动识别不能由用户停止或重复启动，四次耗尽后才允许人工发起新周期。模型只接收整表处理图、球队名和球员姓名；不发送账号、UUID、赛季、日期、场地或其他比赛先验。
- 已实现 60 秒单编辑租约、15 秒心跳、两秒轮询、字段级差异和断网恢复。两端不显示接手、交接或强制接管按钮：首个终端自动取得编辑权，其余终端实时只读并在释放/失效后自动取得。网页完整保留冻结版 `ScoresheetReader` 全视口工作台，小程序为独立分包的原生区域式编辑器与四块纸面得分格。
- 已实现校验摘要绑定、全部 warning 一次确认和原子发布；publication、单场球队/球员统计不可变，旧发布在纠错完成前继续对外。公开统计和 PDF/CSV/XLSX 导出只包含纸面可提供字段。完整协议见 `docs/SCORESHEETS.md`。
- 记录表实现已通过真实 WSL/PostgreSQL 全量后端 104 项（29 项可选外部样本按环境跳过）、记录表/角色专项 18 项、管理站与共享领域包 44 项、Ruff、迁移漂移、OpenAPI 生成、全仓类型检查、管理站生产构建和 Taro `build:weapp`。本地 8088 已应用兼容既有数据库的 `0014_scoresheet_recognition_base_draft_version`，API、网页和识别 worker 均稳定运行；当前未配置 Qwen 密钥，因此真实模型调用和微信开发者工具/真机交互仍按 M4 验收边界保留。
- 本轮统一 `check-wsl` 已通过：真实 PostgreSQL 后端 130 项通过、29 项按环境跳过，管理站 44 项、记录表领域 4 项、小程序首页日历 3 项通过，Ruff、迁移漂移、OpenAPI/TypeScript 同步、全仓类型检查及管理站/小程序生产构建全绿。补充淘汰赛赛季切换竞态回归后，管理站为 45 项通过。
- 最新代码已重新部署到现有 `Ubuntu-24.04` WSL，未执行 `down` 或重建数据卷；`0015_division_operation_lifecycle` 与 `0016_game_winner_feed` 均处于已应用状态，幂等旧数据导入仍为 4 个组别、57 支球队、146 场比赛、3 个标准场地和 8 个标准时段。真实超级管理员登录态已只读打开赛季上线、北大杯历史推导淘汰赛和 45 个模型的高级数据页；测试账号随后重新禁用并设置不可用密码，未确认淘汰赛关系或修改业务数据。
- ScoresheetReader 1.4 语义迁移已收口并重新部署：`0017_scoresheetreader_semantic_v2` 与纯审计回填 `0018_scoresheet_recognition_audit_backfill` 已应用，冻结基线仍为提交 `c27fe987a71637a8f1161a991d3c15f6c0afdeed`。统一检查为 Django/PostgreSQL 135 项通过、29 项可选样本跳过，管理站 125 项、共享记录表领域 4 项、小程序 4 项通过，Ruff、迁移漂移、OpenAPI/TypeScript、全仓类型检查和两端生产构建全绿。隔离演示记录表包含双方各 12 人、19 条逐次得分、12:50 开赛与 21:18 结果；真实浏览器已验证原图/PDF/覆盖层、自动保存、服务端字段级日志、撤销恢复、0 error/3 warning 校验和单次发布确认，未执行发布。微信开发者工具可定位但截图接口返回 `SetIsBorderRequired failed`，无可用视觉树，因此真机画布、键盘和安全区仍按既有边界待人工验收。
- 小程序最终源码已再次通过类型检查、3 项日历测试和完整 Taro 微信构建；编译产物确认首页先渲染连续赛季热图，“赛程赛果 / 淘汰赛”是对阵页首个节点且使用 `position: sticky; top: 0`。微信开发者工具最终前台截图与长列表滚动手势仍需在工具可见会话中人工收口，因此不把该项标成视觉已验证。
- 根级类型检查会先按依赖顺序构建设计变量、`@pkuba/scoresheet-domain` 和 API 客户端，再检查各 workspace；这样干净 CI 的类型检查与后续组件测试都不依赖本机残留的 `dist`。
- 超级管理员正式修改赛程的 UI 收敛到网页后台“赛程编辑”：集中展示比赛、日期、时段、场地、双方、比分、锁定与活动申请影响；男篮、女篮使用不同色系，永久不可调比赛使用独立锁定背景并保留文字状态，保存仍要求勾选核对项和二次确认。小程序仅保留赛程/资料查看及调赛处理，不暴露直接编辑入口。
- 小程序源码的完成标准固定为：TypeScript 检查通过、Taro 微信构建通过，并在微信开发者工具中编译和人工检查本轮受影响页面；工具内检查未完成时不得把前端标记为已验证。

### 2026-08-23 剩余功能审计

结论：V1 的核心业务链路已经具备真实页面、API、PostgreSQL 模型和自动化测试。管理站导航中的赛季、球队与名单、赛程编排/编辑、抽签、淘汰赛、调赛、比赛资料、管理员和高级数据均已接入；小程序的首页、对阵、排名、数据、我的、身份、领队/管理员工作台、任务箱、调赛、比赛资料和记录表也均已接入。后续不再把这些项目列为“未实现”，而按以下三类管理。

#### A. 代码尚未实现，属于上线阻断项

1. **可执行的同批次备份与恢复**：仓库只有要求文档，尚无 PostgreSQL 与私有媒体使用同一 `backup_id` 的备份、加密清单、保留清理、空环境恢复和核验命令。
2. **腾讯 COS 私有存储后端**：当前仍是 Docker 私有 volume；`COS_*` 只是无效占位配置。尚缺本地/COS 可切换 storage backend、私有对象访问、现有 `file_key` 原样迁移、数量与 SHA-256 对账以及失败回滚。
3. **完整赛季归档包**：当前只有赛程草稿 XLSX 和记录表统计导出；尚不能一次导出赛季元信息、组别、球队、名单、签位、赛程、赛果、publication 统计、审计索引和媒体校验清单。
4. **可回滚的正式部署流程**：标签镜像发布已存在，但 `deploy.yml` 仍只校验标签；尚未实现连接 Ubuntu 服务器、部署前备份、迁移、健康切换、失败回滚和部署审计。
5. **生产环境初始化与合成数据阻断**：正式环境必须使用全新数据库和私有媒体存储，通过后台录入真实当前赛季；部署流程尚未强制运行 `check_no_synthetic_public_data`、最后一个超级管理员检查和公开赛季唯一性检查。

#### B. 代码已实现，但尚未完成真实外部验收

1. **微信视觉与真机**：TypeScript、Taro 构建和组件测试已通过，但首页热图、对阵页固定切换栏、任务红点、榜单分页、记录表画布、软键盘、安全区、前后台恢复与跨端只读切换仍需在微信开发者工具和至少一台真机人工验收。自动截图接口仍不兼容，因此不能把“构建通过”写成“视觉已验收”。
2. **Qwen 真实识别**：队列、四次尝试、30 秒重试、10 MP 处理、长调用租约和人工兜底均已实现；当前环境未配置真实凭据，尚未使用真实高清记录表完成一次成功、一次可重试失败和一次四次耗尽的验收。
3. **邮件闭环**：业务 Outbox、固定公邮、去重、重试和耗尽告警均已实现；尚未轮换旧 163 授权码、在 Mailpit 核对实际正文/重试并显式启用真实发送进程。所有邮件仍只发 `pkubaoutward@163.com`。
4. **正式微信和域名**：本地真实 `code2session` 已可用；生产域名、HTTPS、备案、微信 request/uploadFile/downloadFile 合法域名和正式环境登录尚未验收。

#### C. 已明确不做，不属于遗留功能

- 不自动按球队数量生成小组、淘汰赛阶段或应有对阵；管理员继续在现有页面逐项手工设置。
- 数据页本阶段不增加球队/球员详情页；球队展示完整榜单，球员每页 20 人、最多 5 页且只展示前 100 名。
- V1 不实现裁判身份与排班、公众历史赛季、实时临场计分、离线权威副本、微信订阅消息、特殊调赛附件、系统内抽签、跨组排名、高级技术统计、Redis/Celery、微服务或随 `main` 自动上线。

### 下一阶段实施计划

#### 第一批（下一步，P0）：建立“可恢复发布基线”

目标是在不接触正式业务数据的前提下，证明一个固定版本能够从备份在全新的隔离环境中完整恢复。这一批先支持当前私有 volume，避免在尚不能恢复时直接迁移 COS。

1. 新增 Ubuntu 备份命令：进入维护模式，暂停业务写入并排空媒体/识别任务后锁定一个 `backup_id`，保存 PostgreSQL 自定义格式 dump、私有媒体只读快照、版本标签、迁移清单、对象数量/总字节数及 SHA-256 清单；默认只预览，实际执行需显式确认，成功或失败后都必须恢复原服务状态。
2. 新增恢复命令：只允许空的隔离 Compose project，校验清单后恢复同批次数据库和媒体；禁止覆盖当前运行中的生产 volume。
3. 新增恢复验收命令：核对数据库关键模型计数、全部迁移、媒体数量和哈希，抽查已发布记录表原图，并验证 API 健康、公开赛程、PDF/CSV/XLSX 导出。
4. 在本地 WSL 完成一次“备份现有合成开发库 → 恢复到全新隔离库 → 自动核验 → 销毁隔离副本”的演练，记录耗时和结果，但不得改动当前 57 队、146 场的开发库。
5. 更新 `docs/DEPLOYMENT.md`、`README.md`、`AGENTS.md` 和 CI 的脚本静态检查；提交应拆为“备份/恢复实现”和“演练证据/文档”两个可回滚提交。

验收标准：恢复前后关键模型计数一致，媒体数量、总字节数和 SHA-256 全部一致；迁移无缺失；健康检查和指定导出均成功；任何清单不符都必须停止且不能部分覆盖目标环境。

#### 第二批（P0）：私有 COS 与完整归档

1. 增加 `local` / `cos` 两种显式 storage backend，API 与记录表 worker 共享同一配置；COS bucket 保持私有，客户端只取得短期业务授权 URL。
2. 提供幂等迁移工具，保持 `file_key` 不变；先复制、再全量数量/哈希核验、最后切换读取，失败时继续使用本地副本。
3. 增加完整赛季归档命令与超级管理员入口；归档包包含业务 JSON/XLSX、审计索引和媒体清单，不包含环境密钥、OpenID 或令牌摘要。
4. 将备份流程扩展为 PostgreSQL 与 COS 版本化对象的同一恢复点，并在隔离环境再次演练。

#### 第三批（P0）：正式 Ubuntu 发布与空库赛季演练

1. 从固定标签镜像执行：预检 → 备份 → 拉取 → 迁移 → 启动新版本 → 健康检查 → 切换；失败自动保留并恢复上一版本。
2. 在全新生产候选数据库中手工走通：创建赛季 → 配置组别/场地/时段/容量/签位 → 导入真实球队名单 → 编排赛程 → 公开占位符 → 抽签映射 → 分组上线。该验收不新增自动赛制向导。
3. 发布门禁必须包含：无合成数据、无活动异常流程、唯一公开赛季、至少一个有效超级管理员、备份可恢复、生产配置安全检查。
4. 完成域名、HTTPS、备案、最小权限、防火墙和微信合法域名，再做生产微信登录、上传下载、调赛、任务箱和记录表真机冒烟。

#### 第四批（P1）：外部服务、接口与依赖收口

1. 完成真实 Qwen、Mailpit、163 公邮及微信真机的验收矩阵，保留脱敏日志和截图；邮件发送进程只在凭据轮换和 Mailpit 验收后启用。
2. 对其余写接口做风险分级。现有七个不可重复关键命令已支持 `Idempotency-Key`；将调赛响应/撤回/管理员决定、资料替换/审核/删除、赛果纠错等高风险命令纳入同一机制，纯幂等状态标记和带版本冲突保护的低风险编辑可保留现状并记录理由。
3. 对仍可能随赛季增长的管理列表补齐服务端分页；固定规模的赛季配置、组别、场地、时段和签位不为形式统一而分页。
4. 重新执行生产依赖安全审计，优先评估 Taro 升级；不得直接运行破坏性 `npm audit fix --force`。升级后必须重新完成小程序构建、开发者工具编译和真机回归。

当前推荐立即实施第一批。它不依赖域名、COS 密钥、Qwen 密钥或真实赛事数据，却能先消除“数据库或媒体损坏后无法证明可恢复”的最大运营风险，并为后续 COS 迁移和正式服务器部署提供同一套校验基础。

### 当前风险

- 2026-08-19 执行 `npm audit --omit=dev` 时，Taro 4.2.1 的传递依赖报告 16 项告警（3 critical、10 moderate、3 low），关键项没有兼容的自动修复。当前版本可完成本地构建，但不视为生产安全验收通过；上线前必须重新评估 Taro 升级或经过完整回归验证的依赖覆盖，禁止直接执行破坏性 `npm audit fix --force`。
- 本地 `WECHAT_APP_SECRET` 已安全同步到 WSL Django 容器，真实 `code2session`、微信身份恢复和小程序会话已经由“九毛”账号成功使用；2026-08-22 只读核对仍有有效会话。生产域名、生产 AppSecret 注入、微信 request 合法域名和正式环境真机登录尚未验收，因此当前本地成功不能代替 M5 发布验收。
- 本机微信开发者工具的 CLI 服务端口当前关闭；默认启动仍会打开工具，需手动导入 `apps/miniapp`。只有用户自行在工具安全设置中开启服务端口后，才使用 `-UseWechatCli` 自动导入。
- WSL 配置只用于本机测试；生产服务器的 HTTPS、备份、COS、域名和微信真机验收仍属 M5。

## 技术架构

- 后端：Python 3.13、Django 5.2 LTS、Django Ninja、PostgreSQL。
- 小程序：Taro 4.2.1、React 18.3、TypeScript，沿用旧 AppID。
- 管理网站：React 18、TypeScript、Vite。
- 单仓库：`apps/api`、`apps/admin-web`、`apps/miniapp`、`packages/*`。
- API：统一 `/api/v1`；OpenAPI 自动生成 TypeScript 客户端。
- 本地验收：Ubuntu WSL 内用独立 Compose 运行 PostgreSQL、Django/Gunicorn、Caddy 和 Mailpit；Windows 运行微信开发者工具并通过端口代理访问。
- 快速开发：保留 Docker Desktop + Windows 前端监听脚本作为可选路径，不作为最终本地验收基线。
- 生产：单台 Linux 服务器运行 Caddy、Django、记录表 worker、任务进程和 PostgreSQL；当前文件使用私有 Docker volume，完成迁移校验后再切换腾讯 COS 私有桶。
- 暂不采用 Redis、Celery、微服务、Kubernetes 或客户端权威业务规则。

## 赛季、组织与公开状态

- `Season` 状态固定为 `SETUP`、`PRE_DRAW_PUBLIC`、`ACTIVE`、`ARCHIVED`。
- 同时最多一个公开赛季；公众不查询历史赛季，管理员可只读查看和导出归档。
- 新赛季重新创建球队、名单、赛程、签位和领队绑定；旧赛季记录保留在同一数据库并按 `season_id` 隔离。
- 北大杯默认男甲、男乙、女甲、女乙；新生杯默认男篮、女篮；准备阶段可配置。
- 只有超级管理员可以创建赛季或修改基础配置。新赛季可以使用赛事类型默认配置，也可以从任一历史赛季沿用组别、标准场地、容量和签位族；8 个稳定时段使用当前系统默认值。复制绝不带入球队、比赛、身份、比分、草稿或其他业务数据。
- 名称、赛事类型、年份、日期范围、组别、场地、时段、工作日/周末容量、特殊日期覆盖和签位族由同一带版本号的服务端事务保存；赛程草稿及其动态列使用独立版本和审计。`SETUP` 可直接维护；公开或归档后仍允许超级管理员通过影响预览、二次确认和完整审计进入维护模式，普通管理员始终只读。
- 容量只保存周中/周末默认值和按日期/时段的稀疏例外，不保存可失真的已用计数。每日台账实时汇总非 `VOID` 比赛与 `ACTIVE` 预留；容量不被场地数硬截断，也允许降到当前占用以下，既有比赛不自动取消，但必须标记 `over_capacity` 并阻止普通流程继续占用。被业务数据引用的组别、场地和时段不得无影响预览地删除。
- 抽签前可以公开占位符赛程。签位未解析时不得调赛、录入记录表或产生赛果。
- 赛季归档后领队绑定失效，所有业务数据只读。

## 身份与权限

- 普通访客无需登录即可查看当前赛季公开信息。
- 微信仅用于取得稳定 OpenID；用户首次执行身份操作时自行设置唯一昵称。系统不保存姓名、邮箱、手机号或额外显示名，昵称也是管理员网页登录名。
- 领队用同一微信账号认领未被认领的当季球队；一人一队、一队一领队，认领后只能由超级管理员审计纠正。
- 管理员用当前赛季邀请码在小程序注册为普通管理员，初始网页登录密码与注册时的邀请码相同；登录后台后可修改个人密码。邀请码仅保存摘要、可随时轮换并审计，轮换不改变已有密码；已注册管理员是全局身份，不因换季失效。
- 领队绑定与管理员角色相互独立，同一账号可以同时拥有两种身份；后续进入小程序时按 OpenID 自动恢复全部身份。
- 管理网站默认使用小程序扫码确认，个人用户名/密码作为备用方式。扫码挑战五分钟失效并绑定发起浏览器；二维码令牌、浏览器令牌相互分离，只有已登录且当前仍有效的管理员可确认，确认后只能由原浏览器消费一次。
- 超级管理员可升级普通管理员，不能在应用内降级；账号停用是独立操作，且不能停用最后一个超级管理员。
- 普通管理员与超级管理员均可上传比赛资料；首次发布前均可上传/重传记录表、编辑、完整手工录入、在四次识别耗尽后发起新周期、校验和首次发布。活动识别不能停止或重复启动；已发布记录表的重传、纠错、重新发布与强制租约，以及调赛审核/取消、资料审核/删除、赛季、球队、名单、赛程、账号、弃权和危险纠错仍仅超级管理员可操作。

## 赛程导入、编辑与抽签

### 球队与名单

- 新赛季创建后的第一项数据工作是名单导入：下载按实际 `Division` 动态生成的模板，填写后上传审计，再一次性确认整季球队与名单。模板固定含“填写说明”页和每组一页，数据列为“球队标准名称*、球员姓名*、球衣号码（选填）”；数据页不放示例行。
- `Team.name` 是当季标准名称。首次出现的新球队直接创建；不引入独立简称，`short_name` 留空且公开接口回退显示标准名称。同一球队后续始终引用稳定 UUID，重命名不会断开比赛、抽签、排名或领队关系。
- 球队名和球员名必填；球衣号码为文本，只接受 `0–99`、`00` 或空。重复号码只警告；同队重复球员名阻止确认。近似名称只建议，不自动合并；管理员必须统一名称或明确确认是不同球队。
- 首次确认仅允许 `SETUP`、无已确认名单批次且不存在实际小组、签位、抽签、比赛、领队绑定或调赛等下游数据。事务可替换尚未被引用的旧球队和旧名单；任何保护引用、版本变化或创建失败都回滚整批。确认后不提供名单重置或再次导入。
- 在线维护允许新增球队和球员；进行中赛季的球队重命名/停用及既有球员修改/停用先返回维护预览并二次确认。被比赛、抽签、领队或调赛引用的球队不能停用；归档赛季只读。所有写入使用球队/球员版本、稳定 ID、事务和追加审计。

- 准备中的赛季必须先有 `Season`、`Division`、`Team`、`Period`、`PeriodCapacity` 和 `ScheduleSlotFamily`；确认只创建 `CompetitionGroup`、`ParticipantSlot` 和 `Game`，不创建/修改球队或场地，也不写 `DrawAssignment`。
- 系统生成无签名 XLSX V3.2，固定包含“填写说明”“签位定义”“赛程网格”三个可见工作表。签位族由数据库中的组别、阶段、大小写敏感字母和签位数定义；第二页仅展示该配置，不读取其业务内容。工作簿不使用隐藏赛季身份、HMAC 元数据、宏、外部链接或单元格公式；不兼容旧赛季工作簿。
- 赛程网格 A 列为日期、B 列为星期，每一行是一整天；C 列起的时间、自由文本场地、顺序和“仅决赛”可直接编辑。时间必须精确命中赛季已有时段，列必须连续且组合唯一；日期、星期和可填网格均有明确横纵边框。单元格填写 `A1vsA2`，女子比赛追加 `（女）`。
- 每个小组赛/循环赛签位族必须覆盖全部两两对阵；淘汰赛、半决赛、决赛和保级赛每个签位必须且只能出现一次。多批新增只能增加完整阶段或签位族，未出现在新文件中的既有比赛保持不变。
- XLSX 上传先做文件和结构预检，再整表替换同一服务器草稿；非空草稿在页面显式确认，结构错误不产生修改。业务核对覆盖缺失/反向重复对阵、阶段一致性、比赛数量、日期、动态列头、场地占用、容量、同一签位同时参赛、文件安全和并发变化。
- 确认时重新锁定赛季及相关排期资源并复核上传文件 SHA-256，在单一 PostgreSQL 事务中新增记录、写入来源批次、增加赛季版本并记录审计；错误、警告之外的任何创建失败都完整回滚。
- XLSX 不包含 `leader_adjustable`，也不按阶段或文字推断；上传格默认可调。在线草稿可对已填比赛逐场切换“领队可调/不可调”，确认时按草稿值创建，正式赛程仍可在“赛程编辑”继续修改。
- 超级管理员可在 `SETUP` 状态先执行重置预检，再输入赛季名称并二次确认。重置仅删除已确认导入批次创建且仍安全的比赛、签位和小组；任何抽签、赛果、申请、预留、媒体或非导入引用都会阻止整次操作，手工对象始终保留。
- 在线编辑与 XLSX 共用同一日期×时段/场地校验；草稿自动保存采用版本检查和追加审计，确认前不写正式赛程。
- 抽签页面只展示 `group` 非空的小组赛/循环赛初始签位；球队选项来自当前组别的启用 `Team`，不允许手工输入、跨组选择、停用球队或同队重复落位。
- 每次保存必须提交一个组别的完整签位快照，并要求启用球队数与初始签位数一致。服务端锁定赛季、组别、签位、球队、已有映射和受影响比赛，复核赛季版本与预览哈希后，在同一事务中写 `DrawAssignment`、同步比赛双方、增加版本并记录前后审计；任何冲突都回滚整次保存。
- `SETUP` 和 `PRE_DRAW_PUBLIC` 可正常落位；`ACTIVE` 只允许修正未来且仍为未赛、无比分、无任何调赛记录和比赛资料的相关比赛。保存前必须展示签位变化、受影响比赛与公开影响；`ARCHIVED` 只读，更高风险的历史纠错留给独立危险修正流程。

## 调赛不变量

- `Game.leader_adjustable`、`active_reschedule_request_id` 和 `version` 必须分离。
- 截止日取原比赛与目标比赛日期较早者：提交为 D-3 24:00，球队确认为 D-2 24:00；周一至周日为同一周。
- 提交事务同时复核权限、关系、策略、截止、版本与容量，选择首个可用场地，创建申请，锁定原比赛并预留目标场地。
- 状态至少包含：等待对手、等待管理员决定、等待指定球队、等待管理员终审、通过、拒绝、撤回、过期、管理员取消。
- 同周由对手按时同意后自动生效；跨周由管理员直批或指定球队投票，球队按时确认后可无限期等待管理员终审。
- 所有非终态持续持有原比赛锁和目标预留。通过时原子转换；拒绝、撤回、超时或取消时幂等释放。
- 标准容量同时统计正式比赛和有效预留，并保证同一场地唯一。
- 北大杯默认 8 个时段：12:50、14:20、15:50、18:30、18:20、19:50、20:30、20:40；工作日/周末容量分别为 1/3、0/3、0/3、1/1、0/2、0/2、1/1、1/0，并可由特殊日期稀疏覆盖。
- 超级管理员例外不修改容量元信息，不静默损害无关申请，必须二次确认并完整审计。
- 特殊原因调赛及协商失败抽签继续在线下处理；抽签申请必须按照《参赛手册》规定的条件、时限和完整步骤办理，包括按手册要求发送邮件、提交说明或材料，具体要求以参赛手册为准。系统只录入最终结果。

## 排名

- 排名由 Django 根据 PostgreSQL 中当前公开赛季的正式小组赛比分计算，客户端不保存或推断权威顺序。
- 正常比赛胜者 2 分、负者 1 分；弃权胜者 2 分、负者 0 分。未录入比分、未解析参赛方或已作废比赛不计入排名。
- 先比较总积分；同分依次比较相互积分、相互净胜分、相互总得分、总净胜分和总得分。所有正式指标仍相同则显示并列名次，不做跨组排名。
- 小程序按男甲、男乙、女甲、女乙及小组切换，展示排名、赛/胜/负/积分/净胜和交手比分矩阵。

## 公开数据榜单

- 球队榜只使用当前公开赛季中 `COMPLETED` 和 `FORFEIT` 且比分完整的正式比赛；支持按场均得分、总得分、场均失分、场均净胜、胜率、胜场和场次排序。
- 球员榜只使用正常完赛比赛的当前 `ScoresheetPublication` 与稳定 `RosterPlayer`；支持按场均/总得分、出场、首发、1/2/3 分事件及犯规排序。弃权不伪造球员数据，未出场球员保留在名单但不进入榜单。
- 公开 API 分页返回榜单和已发布场次摘要，单场完整统计仅在打开对应比赛后读取；客户端不得从旧 publication、草稿或非正式字段自行累计。
- `seed_public_leaderboard_demo` 默认 dry-run，只有显式确认、有效超级管理员且目标公开赛季没有既有名单/记录表时才运行；中途失败原子回滚并清理新文件。既有未建草稿的记录表来源只复用、不删除或替换。

## 淘汰赛对阵

- 公开对阵由 Django 根据当前公开赛季的正式比赛动态生成，客户端不得依赖旧小程序中硬编码的球队简称或自行推断胜者。
- 小程序“对阵”页顶部固定“赛程赛果 / 淘汰赛”切换栏；淘汰赛按组别展示横向轮次，比赛卡包含日期、时间、场地、双方、比分和胜者，保级赛等附加比赛单独列出。
- `GameWinnerFeed` 明确保存来源比赛、目标比赛、目标主/客位置、已应用胜者和版本。关系禁止跨赛季、跨组别、回指较早比赛、重复目标位置及循环；保级赛和排位赛不自动串联。
- 2026 既有结构先按 `LEGACY_DERIVED` 展示；超级管理员必须在后台预览并确认后才转换为权威关系。之后所有正常发布、弃权和直接赛果写入统一传播胜者。
- 已影响下游的赛果不能直接覆盖。超级管理员先预览，再明确确认级联重置；事务取消受影响调赛、释放预留、清除下游参赛方与赛果并撤回当前发布指针，但保留历史 publication、修订、照片和审计。归档赛季不可纠错。

## 记录表、赛果与媒体

> **范围边界：** 旧小程序和 `ScoresheetReader` 保持只读。电脑端以 `ScoresheetReader` 提交 `c27fe987a71637a8f1161a991d3c15f6c0afdeed` 为冻结基线，完整迁移原界面、编辑方式、领域模型、FIBA 2024 校验、单页 A4 模板坐标与渲染；只替换 PKUBA 认证、数据、租约和同步适配。小程序按手机操作设计无损适配；PostgreSQL 是唯一业务权威。

- 图片以 `GameMediaAsset` 按稳定 game ID 隔离，不通过公开静态路径暴露。媒体类别固定为 `SCORESHEET`、`GROUP_PHOTO` 和 `GAME_PHOTO`；仅超级管理员可上传、审核、替换或软删除，所有变更留审计。
- 一场比赛只有一张当前记录表来源；普通管理员或超级管理员上传后冻结比赛与双方名单先验并创建唯一服务端草稿。首次发布前两类管理员均可重传、编辑、完整手工录入、在四次识别耗尽后发起新周期、校验并发布；活动识别期间任何人都不能停止或重复启动。强制接管以及已发布纠错、重传和重新发布仅超级管理员。
- Qwen 一次识别周期最多四次调用，三次自动重试的默认等待均为 30 秒；服务商给出更长 `Retry-After` 时优先。网络、超时、429、5xx、无效 JSON 和 Schema 错误可重试。模型只收到处理后的完整图片、球队名和球员姓名。
- 网页和小程序共用草稿版本、事件序列、校验报告、识别任务和 publication；两秒轮询，不引入 WebSocket。60 秒单编辑租约按 15 秒心跳续期，两端自动取得/释放并在失效后自动恢复，不暴露手工接手、交接或强制接管按钮。
- 网页完整保留冻结版 `ScoresheetReader` 全视口工作台和原有功能，并接入 750 ms 自动保存；小程序使用原生分区步骤、原图/标准表单视图、四角对齐和四块纸面累计得分格，不使用 `web-view`，不做实时临场计分，且通过无损投影保留未在手机界面展开的原表字段。
- 六个区域只用于小程序导航、问题定位和识别差异，不设人工逐区确认门槛。发布前服务端 error 必须为零；全部 warning 只做一次确认，并要求所有摘要和租约仍有效且比分不为平局。
- 发布事务一次创建不可变 publication、球队/球员单场统计，更新正式比分/比赛状态/来源审核并写完整审计；任一步失败全部回滚。弃权继续走超级管理员现有 20:0 流程。
- 领队只能查看本队比赛当前 publication 绑定的原图；公众只能看当前 publication 的纸面统计。纠错期间旧 publication 和旧原图继续对外，直至新版本发布成功。
- 公开统计不提供命中率、篮板、助攻、抢断、盖帽、失误、上场时间或正负值。管理员可导出标准记录表 PDF、单场 CSV 和赛季 XLSX。
- 当前私有文件仍在 Docker volume；COS 切换、同批次数据库/媒体备份和恢复演练要求见 `docs/SCORESHEETS.md` 与 `docs/DEPLOYMENT.md`。

## 产品界面

小程序底部入口：首页、对阵、排名、数据、我的；自定义底栏使用 30rpx 加粗文字并由每个页面主动同步当前项。“我的”显示 1–99/99+ 的待处理任务红点，打开任务只记录已查看，只有业务进入终态才减少数字。首页首个数据模块是只读赛季热图。“对阵”同时承载赛程赛果与淘汰赛晋级图，其切换栏始终固定在页面顶部；点击比赛进入私有资料页。“我的”静默恢复已注册微信身份，并统一承载任务箱、两级球队认领、管理员注册及角色工作台入口。

视觉采用白色/暖灰底、深色正文和单一 PKUBA 红；小程序首页使用由旧项目原始 Logo 确定性生成的透明资产。比赛信息优先，避免渐变、重阴影、无意义卡片及解释界面自身的冗余说明文字。

## 实施顺序

1. 文档、GPL-3.0、单仓库、开发脚本与 CI。
2. PostgreSQL、核心 Django 模型、样例数据和公开赛程 API。
3. 管理站与小程序读取真实 API 的纵向主干。
4. 赛季/球队/名单、XLSX、在线网格、抽签与归档。
5. 微信身份、管理员、领队和权限。
6. 调赛事务、预留、任务箱和邮件 Outbox。
7. 记录表 PostgreSQL worker、冻结版 `ScoresheetReader` 电脑端迁移、小程序无损适配、跨端编辑、校验发布、统计和导出；旧仓库保持只读。
8. 安全、COS、备份恢复、域名与手动版本发布。

## 交付和发布

- GitHub 私有仓库，单人阶段直接推送 `main`，不要求 PR、审核或分支保护。
- GitHub Actions 对每次推送运行测试和构建，但不作为推送门禁。
- `main` 不自动上线；创建明确版本标签后手动触发生产发布。
- 部署顺序：备份 → 拉取固定镜像 → 迁移 → 健康检查 → 切换；失败保留上一版本。
- PostgreSQL 每日加密备份，保留 30 份日备份与 12 份月备份，并定期恢复演练。
- 邮件使用 PostgreSQL Outbox 和独立任务进程；唯一收件人固定为协会公邮 `pkubaoutward@163.com`，永不配置协会成员或领队邮箱。业务事务与 Outbox 入队同事务，SMTP 失败只重试并在耗尽后创建超级管理员异常任务，不回滚业务；业务正文已接线，真实发送进程默认禁用。
- 生产域名使用 `api.<domain>` 和 `admin.<domain>`。大陆服务器需在正式开放前完成备案。

## V1 不包含

- 裁判注册、登录、个人赛程或排班。
- 旧 OpenID、密码、进行中申请或旧赛季数据迁移。
- 公众历史赛季查询。
- 实时临场计分、离线权威副本和微信订阅消息。
- 特殊原因调赛附件、系统内抽签、跨组排名和高级技术统计。
- 多人实时协同编辑、Redis、Celery、微服务和自动随 `main` 上线。
