# PKUBA 开发约束

- 开工前读 `Plan.md` 和规格文档 A、6.6、9、11 章；旧项目只读，禁止迁移秘密或身份数据。
- Django/PostgreSQL 是唯一业务权威；写操作使用稳定 ID、版本号、事务、幂等保护和审计。高风险命令按 `docs/API_RELIABILITY.md` 接受 `Idempotency-Key`，只保存摘要并与业务写入同事务；可增长列表使用统一分页结构，单页不得超过 100 条。
- 球队名单是新赛季第一步：每季只允许一次 `SETUP` 全量导入，首次出现的标准球队名称直接创建，不匹配历史名称；确认后永久关闭重导，后续只在线维护。`Team.short_name` 留空，球衣号以文本保存 `00`，近似名称不得自动合并。
- `leader_adjustable`、活动申请锁和目标预留必须分离；非终态不得释放锁。
- 赛季状态只保留 `SETUP`、`PUBLISHED`、`ARCHIVED`；同一时间至多一个公开赛季。组别不再设置独立运营开关，调赛、资料、记录表和赛果是否可操作由赛季已公开、比赛双方已落位及各业务自身条件实时派生；`ARCHIVED` 是不可逆只读终态。
- 小组初始签位按组别整组确认；淘汰赛、半决赛、决赛和保级赛签位均由超级管理员在“抽签映射”逐场手工录入。上一轮胜队只用于动态校验和越级警告，任何赛果写入或纠正都不得自动填写、替换或级联清空后续比赛；不一致签位标记待复核并保留原数据。
- 高级数据页使用静态模型白名单；工作流、身份凭据、发布、统计和审计只读。只允许 `SETUP` 且无引用的普通主数据经校验、预览、二次确认和审计修改；名称、状态、日期等人类可读字段优先，UUID、publication/revision、哈希和令牌靠后。
- 新赛季默认标准场地为五四东一至东三；超级管理员可维护有序启用列表，普通调赛只从启用的标准场地按顺序自动分配。
- 标准时段使用系统预设的 8 个稳定代码，不提供任意增删；比赛另存实际时间，容量和调赛始终按标准时段归属。
- 容量只保存周中/周末默认值和管理员设置的特殊日期稀疏覆盖；特殊日期默认空，禁止从实际比赛反推。已用量实时统计正式比赛与 `ACTIVE` 预留，不持久化计数。超级管理员例外不得静默影响无关申请。
- 旧 `ScoresheetReader` 和旧小程序保持只读；电脑端记录表编辑器以 `ScoresheetReader` 提交 `c27fe987a71637a8f1161a991d3c15f6c0afdeed` 为冻结基线，完整保留其界面、编辑方式、领域规则、坐标与渲染；识别图片准备与 Qwen 请求模式以提交 `d631601` 为基线。只在本仓库接入 Django/PostgreSQL、自动租约与跨端同步；小程序按手机交互适配，但必须无损读写同一份结构化草稿。不得迁移秘密、身份数据或运行时副本。
- 抽签申请只作线下说明；条件、时限、邮件、材料和办理步骤必须指向《参赛手册》，并以手册为准。
- 普通管理员和超级管理员都可上传比赛资料；首次发布前都可上传/重传记录表、编辑、完整手工录入、在四次识别耗尽后发起新周期、校验并首次发布。识别执行或等待自动重试期间任何人都不能停止或重复启动；已发布记录表的重传、纠错、重新发布和强制租约仅超级管理员。
- 比赛资料固定分为记录表、比赛合照、其他照片（既有 `GAME_PHOTO`）；所有图片以临时文件分块接收、累计真实字节和 SHA-256，再由 Pillow 安全解码并记录尺寸，不设应用层字节上限或固定分辨率门槛。Pillow 解压炸弹保护仍返回 413。非记录表图片审核和删除仅超级管理员；记录表每场一张当前来源，替换保留旧文件、publication 与审计。
- 任务箱红点按 `OPEN` 业务任务计数，阅读不关闭；只由业务终态幂等关闭。邮件唯一收件人固定为 `pkubaoutward@163.com`，永不保存成员/领队邮箱；调赛状态和管理员异常与业务事务同事务写入去重 Outbox，历史任务重建不得补发邮件。旧授权码禁止迁移，真实 SMTP 进程只可在轮换凭据并完成验收后显式启用。
- 账号只保存唯一昵称，不保存姓名、邮箱或手机号；领队绑定与管理员角色可同时存在。
- 小程序身份入口只放在“我的”；首页只展示赛事信息。组别只写男甲/男乙/女甲/女乙，不重复写男篮/女篮。
- 首页比赛热图必须连续覆盖首末场之间每一天并显示 0；“对阵”的赛程赛果/淘汰赛切换栏固定在页面顶部。
- “对阵”中的淘汰赛只从后端正式比赛和比分生成；禁止客户端硬编码球队、胜者或冠军。
- 领队与管理员操作必须调用服务端角色接口；客户端不得自行推进调赛状态或直接覆盖赛程。
- 超级管理员直接编辑赛程的正式 UI 只放网页后台；小程序不暴露直接修改入口。
- 管理员在小程序注册时必须同时提交当季邀请码并自行设置网页密码；密码至少 4 个字符，不做其他强度限制。邀请码和密码分别只存摘要，轮换邀请码不得改已有密码。
- 网页扫码登录必须使用五分钟一次性挑战：二维码令牌、浏览器会话令牌和数据库摘要分离，只允许有效管理员确认并由原浏览器单次消费。
- 组内排名只能由后端根据正式比分计算；正常胜/负为 2/1 分，弃权为 2/0 分，不做跨组排名。
- 公开球队榜只聚合正式比分并完整展示所选组别；球员榜只聚合当前 publication 且绑定稳定名单 ID 的纸面统计，每页 20 人、最多 5 页，仅展示前 100 名。合成榜单数据必须带审计标记，生产上线前由 `check_no_synthetic_public_data` 阻断并整批清除。
- 旧 Backup 导入只允许读取 Private/Team/Schedule 三份公开骨架，禁止导入 OpenID、人员、申请、照片或秘密。
- 生产媒体只存 Ubuntu 私有 Docker volume，不接入 COS。赛季数据包不得包含 OpenID、密码/令牌摘要或部署密钥；未加密全系统备份属于敏感文件，只能由超级管理员在 HTTPS/localhost 生成和下载。
- 归档包最多暂存 24 小时，确认下载并外部保存后立即删除。只有 `ARCHIVED` 赛季且最终数据包、照片包完整时才能清理照片；只删物理文件，媒体行、哈希、publication 引用和审计永久保留。
- `restore_system_backup` 只允许空的 `pkuba_restore_*` 隔离数据库和空媒体目录；禁止指向当前环境或生产卷。
- V1 暂不实现裁判、公众历史、实时计分、微信订阅消息、Redis/Celery 或微服务；记录表识别使用独立 PostgreSQL worker。
- 业务规则改变时同步更新 `Plan.md`、规格、模型和测试。
- 本地验收以 Ubuntu WSL 为准：`./scripts/deploy-wsl.ps1`；后端检查用 `./scripts/check-wsl.ps1`，全仓检查用 `./scripts/check.ps1`。
- 每次修改 `apps/miniapp/src` 后，交付前必须运行小程序 `typecheck` 和 `build:weapp`，并在微信开发者工具中编译、打开本轮受影响页面检查真实样式；无法完成工具内检查时必须明确报告阻塞，不能只凭构建通过宣称前端已验证。
