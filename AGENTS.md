# PKUBA 开发约束

- 开工前读 `Plan.md` 和规格文档 A、6.6、9、11 章；旧项目只读，禁止迁移秘密或身份数据。
- Django/PostgreSQL 是唯一业务权威；写操作使用稳定 ID、版本号、事务、幂等保护和审计。
- `leader_adjustable`、活动申请锁和目标预留必须分离；非终态不得释放锁。
- 标准容量统计正式比赛与有效预留且场地唯一；超级管理员例外不得静默影响无关申请。
- `ScoresheetReader` 尚未完成：未经用户明确恢复待办，不得复制或迁移其代码，不实现结构化记录表功能。
- 抽签申请只作线下说明；条件、时限、邮件、材料和办理步骤必须指向《参赛手册》，并以手册为准。
- 照片功能已恢复：固定分为记录表、比赛合照、其他照片（既有 `GAME_PHOTO`）；服务端必须解码图片并记录真实尺寸，但不设固定分辨率门槛；记录表由上传者确认、管理员人工审核，替换保留旧文件与审计记录。
- 账号只保存唯一昵称，不保存姓名、邮箱或手机号；领队绑定与管理员角色可同时存在。
- 小程序身份入口只放在“我的”；首页只展示赛事信息。组别只写男甲/男乙/女甲/女乙，不重复写男篮/女篮。
- “对阵”中的淘汰赛只从后端正式比赛和比分生成；禁止客户端硬编码球队、胜者或冠军。
- 领队与管理员操作必须调用服务端角色接口；客户端不得自行推进调赛状态或直接覆盖赛程。
- 超级管理员直接编辑赛程的正式 UI 只放网页后台；小程序不暴露直接修改入口。
- 管理员初始网页密码等于注册时邀请码，后台可改；邀请码只存摘要，轮换不得改已有密码。
- 组内排名只能由后端根据正式比分计算；正常胜/负为 2/1 分，弃权为 2/0 分，不做跨组排名。
- 旧 Backup 导入只允许读取 Private/Team/Schedule 三份公开骨架，禁止导入 OpenID、人员、申请、照片或秘密。
- V1 暂不实现裁判、公众历史、OCR、实时计分、微信订阅消息、Redis/Celery 或微服务。
- 业务规则改变时同步更新 `Plan.md`、规格、模型和测试。
- 本地验收以 Ubuntu WSL 为准：`./scripts/deploy-wsl.ps1`；后端检查用 `./scripts/check-wsl.ps1`，全仓检查用 `./scripts/check.ps1`。
- 每次修改 `apps/miniapp/src` 后，交付前必须运行小程序 `typecheck` 和 `build:weapp`，并在微信开发者工具中编译、打开本轮受影响页面检查真实样式；无法完成工具内检查时必须明确报告阻塞，不能只凭构建通过宣称前端已验证。
