# PKUBA 开发约束

## 范围与数据

- 只修改本仓库；旧小程序和 `ScoresheetReader` 只读。
- Django/PostgreSQL 是唯一业务权威。写操作使用稳定 ID、版本校验、事务、幂等和审计。
- 不提交 `.env`、密钥、OpenID、生产数据库、名单或照片。账号不保存姓名、邮箱或手机号。
- 不覆盖 `docs/INDEPENDENT_TEST_PLAN_AND_RESULTS.md`；测试结果由独立测试任务维护。
- 当前处于测试阶段：不得连接生产服务器、创建生产标签或执行生产切流。
- legacy、seed 和本地管理员工具仅测试期保留；上线门禁前必须彻底删除。部署脚本不得调用这些工具。

## 核心不变量

- 赛季状态为 `SETUP`、`PUBLISHED`、`ARCHIVED`；至多一个公开赛季。
- 所有关联对象必须属于同一赛季。不得依赖客户端或单个调用方保证跨赛季一致性。
- `ARCHIVED` 整体只读。唯一例外：超级管理员可在网页端明确确认后纠正已有 publication 的记录表；必须使用专用租约、重新校验、生成新 publication 并审计。
- 超级管理员可升级普通管理员，也可降级其他超级管理员；禁止自我降级，保护最后一个有效超级管理员。
- 调赛永久政策、活动申请锁和目标预留必须分离；所有非终态持续持锁。
- 调赛目标场地在生效前仅服务端可见；申请接口、任务、邮件和管理页面不得泄露。
- 容量由默认规则和特殊日期稀疏覆盖组成；已用量实时计算比赛与有效预留，不持久化计数。
- 标准时段使用 8 个稳定代码；标准场地默认五四东一至东三，超级管理员可维护有序列表。
- 比分、积分、排名、统计和调赛状态只能由服务端计算或迁移。

## 记录表、媒体与归档

- 记录表使用单一结构化草稿、租约、不可变 revision/publication 和服务端校验。
- 已发布记录表的纠错、重传和重新发布仅限超级管理员；记录表原图不可删除。
- 每场当前记录表和比赛合照各最多一张；其他照片可多张。照片不审核、不排序，替换和软删除必须留痕。
- 图片上传必须先持久化暂存，再原子晋升为正式资产；未完成暂存阻止部署、全系统备份和赛季照片清理。
- Qwen 图片链路保持 8 MP 小图放大目标、180 秒 SDK 网络超时和 20,000,000 字节 Data URI 保护；未经用户新授权不得发起付费调用。
- 全系统备份捕获数据库与媒体集合时必须持独占写围栏。归档清理只删除物理照片，元数据、哈希、publication 和审计永久保留。

## 前端与接口

- 小程序身份入口只放在“我的”；普通用户不得看到管理字段、UUID、哈希或内部代码。
- 首页热图固定显示北京时间当前周前后二周，共 35 天；无比分比赛不显示 `VS/vs`。
- 所有比赛入口进入统一详情页；管理员从任何入口进入都获得相同资料权限。
- OpenAPI 生成 TypeScript 客户端；禁止手写与服务端冲突的权限、状态机或排序规则。
- 小程序生产构建必须显式提供 HTTPS API 和管理站地址，禁止回退到 localhost。

## 工作流

- 共享工作区不执行 reset、clean 或覆盖式恢复；保留无关改动和未跟踪测试报告。
- 优先小步修改，先跑聚焦测试，再跑全仓门槛。构建成功不能替代数据库、浏览器或微信开发者工具验收。
- 常用命令：
  - 本地部署：`./scripts/deploy-wsl.ps1 -SkipInstall`
  - 后端/全仓检查：`./scripts/check-wsl.ps1`
  - 类型检查：`npm run typecheck`
  - 全部前端测试：`npm test`
  - 双端构建：`npm run build`
- 修改 `apps/miniapp/src` 后必须完成 typecheck、`build:weapp` 和微信开发者工具人工检查；工具受阻时如实报告。
- 生产最终采用同机 blue/green；普通故障只回切应用，只有确认数据损坏才成对恢复 DB、媒体和归档。
