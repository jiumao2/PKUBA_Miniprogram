{
  "name": "pkuba",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "workspaces": [
    "apps/admin-web",
    "apps/miniapp",
    "packages/api-client",
    "packages/scoresheet-domain",
    "packages/design-tokens"
  ],
  "scripts": {
    "dev": "concurrently -n admin,miniapp -c red,white \"npm:dev:admin\" \"npm:dev:miniapp\"",
    "dev:admin": "npm --workspace @pkuba/admin-web run dev",
    "dev:miniapp": "npm --workspace @pkuba/miniapp run dev:weapp",
    "generate:api": "npm --workspace @pkuba/api-client run generate",
    "build": "npm run build:packages && npm --workspace @pkuba/admin-web run build && npm --workspace @pkuba/miniapp run build:weapp",
    "build:packages": "npm --workspace @pkuba/design-tokens run build && npm --workspace @pkuba/scoresheet-domain run build && npm --workspace @pkuba/api-client run build",
    "test": "npm --workspace @pkuba/scoresheet-domain run test && npm --workspace @pkuba/admin-web run test && npm --workspace @pkuba/miniapp run test",
    "typecheck": "npm --workspace @pkuba/scoresheet-domain run build && npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "openapi-typescript": "^7.10.1",
    "typescript": "^5.9.2"
  }
}
